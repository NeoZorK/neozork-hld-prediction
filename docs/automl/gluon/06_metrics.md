# Метрики and оценка качества in AutoML Gluon

**Author:** Shcherbyna Rostyslav
**Дата:** 2024

## Why метрики критически важны

**Почему 80% ML-проектов терпят неудачу из-за неправильного выбора метрик?** Потому что метрики - это единственный способ понять, работает ли ваша модель. Это как медицинские анализы - без них нельзя поставить диагноз.

### Что происходит без правильных метрик?
- **Ложная уверенность**: Модель кажется хорошей, но работает плохо
- **Неправильные решения**: Выбираете плохую модель вместо хорошей
- **Потеря времени**: Тратите месяцы on неэффективные подходы
- **Бизнес-провалы**: Модель not решает реальные задачи

### Что дает правильный выбор метрик?
- **Точная оценка**: Вы точно знаете качество модели
- **Правильный выбор**: Выбираете лучшую модель for задачи
- **Быстрая итерация**: Быстро находите проблемы and исправляете их
- **Бизнес-успех**: Модель действительно помогает бизнесу

## Введение in метрики

<img src="images/optimized/metrics_comparison.png" alt="Сравнение метрик" style="max-width: 100%; height: auto; display: block; margin: 20px auto;">
*Рисунок 1: Сравнение метрик for классификации and регрессии*

<img src="images/optimized/metrics_detailed.png" alt="Детальная визуализация метрик" style="max-width: 100%; height: auto; display: block; margin: 20px auto;">
*Рисунок 2: Детальная визуализация метрик - ROC Curve, Precision-Recall, Confusion Matrix, Accuracy vs Threshold, F1 Score vs Threshold*

**Почему метрики - это язык машинного обучения?** Потому что они переводят сложные алгоритмы in понятные числа. Это как переводчик между техническими деталями and бизнес-результатами.

Метрики in AutoML Gluon используются for:
- **Оценки качества моделей**: Понимание того, насколько хорошо работает модель
- **Сравнения различных алгоритмов**: Выбор лучшего алгоритма for задачи
- **Выбора лучшей модели**: Автоматический отбор оптимальной модели
- **Мониторинга производительности**: Отслеживание качества in продакшене

## Метрики for классификации

<img src="images/optimized/robustness_analysis.png" alt="Метрики классификации" style="max-width: 100%; height: auto; display: block; margin: 20px auto;">
*Рисунок 3: Метрики for задач классификации and их интерпретация*

**Почему классификация требует особых метрик?** Потому что здесь важны not только правильные ответы, но and типы ошибок. Ложные срабатывания and пропуски имеют разную цену.

### 🎯 Ключевые концепции метрик классификации

**Почему важно понимать типы ошибок?** Потому что разные ошибки имеют разную стоимость:

- **True Positive (TP)**: Правильно предсказанные положительные случаи
- **True Negative (TN)**: Правильно предсказанные отрицательные случаи
- **False Positive (FP)**: Ложные срабатывания (ошибки I рода)
- **False Negative (FN)**: Пропуски (ошибки II рода)
- **Precision**: Точность - доля правильных среди предсказанных положительных
- **Recall**: Полнота - доля найденных среди всех положительных
- **F1-Score**: Гармоническое среднее precision and recall

### Базовые метрики

#### Accuracy (Точность)
**Почему Accuracy - самая популярная метрика?** Потому что она интуитивно понятна - это просто процент правильных ответов.

**Когда Accuracy вводит in заблуждение?**
- При несбалансированных данных (99% одного класса)
- При разной важности ошибок (медицинская диагностика)
- При небольшом количестве данных

```python
# Процент правильных предсказаний
from sklearn.metrics import accuracy_score

accuracy = accuracy_score(y_true, y_pred)
print(f"Accuracy: {accuracy:.4f}")
```

**🔧 Детальное description параметров accuracy_score:**

**function accuracy_score:**
- **Назначение**: Вычисление точности классификации (процент правильных предсказаний)
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **Тип**: array-like
 - **description**: Массив истинных меток классов
 - **examples**: [0, 1, 1, 0, 1], ['cat', 'dog', 'cat']
 - **`y_pred`**: Предсказанные метки классов
 - **Тип**: array-like
 - **description**: Массив предсказанных меток классов
 - **examples**: [0, 1, 0, 0, 1], ['cat', 'dog', 'dog']
 - **`normalize`**: Нормализация результата (on умолчанию True)
 - **Тип**: bool
 - **description**: True - возвращает долю правильных предсказаний, False - количество
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении точности
- **Возвращаемое значение**: float - точность классификации (0.0 - 1.0)
- **Использование**:
 - **Быстрая оценка**: Первоначальная оценка качества модели
 - **Сравнение моделей**: Сравнение разных алгоритмов
 - **Мониторинг**: Отслеживание качества во время обучения

**Почему Accuracy может быть обманчивой?**
- Модель может просто предсказывать мажоритарный класс
- not показывает, какие ошибки делает модель
- not учитывает важность разных типов ошибок

#### 🔧 Детальное description параметров метрик классификации

**Метрика Accuracy:**
- **Что означает**: Процент правильных предсказаний from общего количества
- **Формула**: `(TP + TN) / (TP + TN + FP + FN)`
- **Диапазон значений**: `[0, 1]` (0% - 100%)
- **Когда использовать**:
 - **Сбалансированные данные**: Когда классы примерно равны on количеству
 - **Простые задачи**: Когда все ошибки одинаково важны
 - **Быстрая оценка**: for первоначальной оценки качества
- **Когда not использовать**:
 - **Несбалансированные данные**: Когда один класс значительно больше
 - **Критичные ошибки**: Когда разные типы ошибок имеют разную важность
 - **Медицинская диагностика**: Когда ложные отрицания опаснее ложных положиний
- **Практические examples**:
 - **Хорошая точность**: `> 0.9` (90%+)
 - **Приемлемая точность**: `0.8-0.9` (80-90%)
 - **Плохая точность**: `< 0.8` (< 80%)
- **Ограничения**:
 - **not показывает типы ошибок**: not различает ложные положиния and отрицания
 - **Мажоритарный класс**: Может быть высокой при предсказании только одного класса
 - **not учитывает важность**: Все ошибки считаются одинаково важными

#### Precision (Точность)
**Почему Precision критически важен?** Потому что он показывает, насколько можно доверять положительным предсказаниям модели.

**Когда Precision особенно важен?**
- Медицинская диагностика (ложные диагнозы опасны)
- Обнаружение мошенничества (ложные обвинения дороги)
- Спам-фильтры (важные письма not должны попадать in спам)

```python
# Доля правильно предсказанных положительных случаев
from sklearn.metrics import precision_score

precision = precision_score(y_true, y_pred, average='binary')
print(f"Precision: {precision:.4f}")

# for многоклассовой классификации
precision_macro = precision_score(y_true, y_pred, average='macro') # Среднее on классам
precision_micro = precision_score(y_true, y_pred, average='micro') # Глобальное среднее
```

**🔧 Детальное description параметров precision_score:**

**function precision_score:**
- **Назначение**: Вычисление точности (precision) for классификации
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **Тип**: array-like
 - **description**: Массив истинных меток классов
 - **examples**: [0, 1, 1, 0, 1], ['cat', 'dog', 'cat']
 - **`y_pred`**: Предсказанные метки классов
 - **Тип**: array-like
 - **description**: Массив предсказанных меток классов
 - **examples**: [0, 1, 0, 0, 1], ['cat', 'dog', 'dog']
 - **`average`**: Тип усреднения (on умолчанию 'binary')
 - **Тип**: str or None
 - **description**: Способ усреднения for многоклассовой классификации
 - **Варианты**: 'binary', 'micro', 'macro', 'weighted', 'samples', None
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении precision
 - **`zero_division`**: Значение при делении on ноль (on умолчанию 'warn')
 - **Тип**: str or float
 - **description**: Что возвращать когда precision not определен
 - **Варианты**: 'warn', 0, 1
- **Возвращаемое значение**: float or array - precision for каждого класса
- **Типы усреднения**:
 - **`'binary'`**: for бинарной классификации (on умолчанию)
 - **`'micro'`**: Глобальное среднее (учитывает количество примеров)
 - **`'macro'`**: Среднее арифметическое on классам (равные веса)
 - **`'weighted'`**: Среднее взвешенное on количеству примеров
 - **`'samples'`**: Precision for каждого примера отдельно
 - **`None`**: Precision for каждого класса отдельно
- **Использование**:
 - **Критичные ложные положиния**: Когда ложные срабатывания дороги
 - **Медицинская диагностика**: Когда ложные диагнозы опасны
 - **Обнаружение мошенничества**: Когда ложные обвинения дороги

**Почему нужны разные типы усреднения?**
- **macro**: Каждый класс имеет равный вес (хорошо for несбалансированных данных)
- **micro**: Учитывает количество примеров in каждом классе (хорошо for сбалансированных данных)

**Метрика Precision:**
- **Что означает**: Доля правильно предсказанных положительных случаев from всех предсказанных положительных
- **Формула**: `TP / (TP + FP)`
- **Диапазон значений**: `[0, 1]` (0% - 100%)
- **Когда использовать**:
 - **Критичные ложные положиния**: Когда ложные срабатывания дороги
 - **Медицинская диагностика**: Когда ложные диагнозы опасны
 - **Обнаружение мошенничества**: Когда ложные обвинения дороги
 - **Спам-фильтры**: Когда важные письма not должны попадать in спам
- **Когда not использовать**:
 - **Критичные ложные отрицания**: Когда пропуски опаснее ложных срабатываний
 - **Несбалансированные данные**: Когда положительный класс очень редкий
- **Практические examples**:
 - **Отличная точность**: `> 0.95` (95%+)
 - **Хорошая точность**: `0.8-0.95` (80-95%)
 - **Приемлемая точность**: `0.6-0.8` (60-80%)
 - **Плохая точность**: `< 0.6` (< 60%)
- **parameter `average`**:
 - **`'binary'`**: for бинарной классификации (on умолчанию)
 - **`'macro'`**: Среднее арифметическое on классам (равные веса)
 - **`'micro'`**: Глобальное среднее (веса on количеству примеров)
 - **`'weighted'`**: Среднее взвешенное on количеству примеров
- **Выбор типа усреднения**:
 - **Несбалансированные данные**: Используйте `'macro'`
 - **Сбалансированные данные**: Используйте `'micro'`
 - **Важны все классы**: Используйте `'macro'`
 - **Важен общий результат**: Используйте `'micro'`

#### Recall (Полнота)
```python
# Доля положительных случаев, которые были правильно предсказаны
from sklearn.metrics import recall_score

recall = recall_score(y_true, y_pred, average='binary')
print(f"Recall: {recall:.4f}")

# for многоклассовой классификации
recall_macro = recall_score(y_true, y_pred, average='macro')
recall_micro = recall_score(y_true, y_pred, average='micro')
```

**🔧 Детальное description параметров recall_score:**

**function recall_score:**
- **Назначение**: Вычисление полноты (recall) for классификации
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **Тип**: array-like
 - **description**: Массив истинных меток классов
 - **examples**: [0, 1, 1, 0, 1], ['cat', 'dog', 'cat']
 - **`y_pred`**: Предсказанные метки классов
 - **Тип**: array-like
 - **description**: Массив предсказанных меток классов
 - **examples**: [0, 1, 0, 0, 1], ['cat', 'dog', 'dog']
 - **`average`**: Тип усреднения (on умолчанию 'binary')
 - **Тип**: str or None
 - **description**: Способ усреднения for многоклассовой классификации
 - **Варианты**: 'binary', 'micro', 'macro', 'weighted', 'samples', None
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении recall
 - **`zero_division`**: Значение при делении on ноль (on умолчанию 'warn')
 - **Тип**: str or float
 - **description**: Что возвращать когда recall not определен
 - **Варианты**: 'warn', 0, 1
- **Возвращаемое значение**: float or array - recall for каждого класса
- **Типы усреднения**:
 - **`'binary'`**: for бинарной классификации (on умолчанию)
 - **`'micro'`**: Глобальное среднее (учитывает количество примеров)
 - **`'macro'`**: Среднее арифметическое on классам (равные веса)
 - **`'weighted'`**: Среднее взвешенное on количеству примеров
 - **`'samples'`**: Recall for каждого примера отдельно
 - **`None`**: Recall for каждого класса отдельно
- **Использование**:
 - **Критичные ложные отрицания**: Когда пропуски опаснее ложных срабатываний
 - **Медицинская диагностика**: Когда пропуск болезни опаснее ложного диагноза
 - **Обнаружение мошенничества**: Когда пропуск мошенничества дороже ложных обвинений

**Метрика Recall:**
- **Что означает**: Доля положительных случаев, которые были правильно предсказаны
- **Формула**: `TP / (TP + FN)`
- **Диапазон значений**: `[0, 1]` (0% - 100%)
- **Когда использовать**:
 - **Критичные ложные отрицания**: Когда пропуски опаснее ложных срабатываний
 - **Медицинская диагностика**: Когда пропуск болезни опаснее ложного диагноза
 - **Обнаружение мошенничества**: Когда пропуск мошенничества дороже ложных обвинений
 - **Поиск информации**: Когда важно найти все релевантные документы
- **Когда not использовать**:
 - **Критичные ложные положиния**: Когда ложные срабатывания дороги
 - **Несбалансированные данные**: Когда положительный класс очень редкий
- **Практические examples**:
 - **Отличная полнота**: `> 0.95` (95%+)
 - **Хорошая полнота**: `0.8-0.95` (80-95%)
 - **Приемлемая полнота**: `0.6-0.8` (60-80%)
 - **Плохая полнота**: `< 0.6` (< 60%)
- **parameter `average`**:
 - **`'binary'`**: for бинарной классификации (on умолчанию)
 - **`'macro'`**: Среднее арифметическое on классам (равные веса)
 - **`'micro'`**: Глобальное среднее (веса on количеству примеров)
 - **`'weighted'`**: Среднее взвешенное on количеству примеров
- **Выбор типа усреднения**:
 - **Несбалансированные данные**: Используйте `'macro'`
 - **Сбалансированные данные**: Используйте `'micro'`
 - **Важны все классы**: Используйте `'macro'`
 - **Важен общий результат**: Используйте `'micro'`

#### F1-Score
```python
# Гармоническое среднее precision and recall
from sklearn.metrics import f1_score

f1 = f1_score(y_true, y_pred, average='binary')
print(f"F1-Score: {f1:.4f}")

# for многоклассовой классификации
f1_macro = f1_score(y_true, y_pred, average='macro')
f1_micro = f1_score(y_true, y_pred, average='micro')
```

**🔧 Детальное description параметров f1_score:**

**function f1_score:**
- **Назначение**: Вычисление F1-меры (гармоническое среднее precision and recall)
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **Тип**: array-like
 - **description**: Массив истинных меток классов
 - **examples**: [0, 1, 1, 0, 1], ['cat', 'dog', 'cat']
 - **`y_pred`**: Предсказанные метки классов
 - **Тип**: array-like
 - **description**: Массив предсказанных меток классов
 - **examples**: [0, 1, 0, 0, 1], ['cat', 'dog', 'dog']
 - **`average`**: Тип усреднения (on умолчанию 'binary')
 - **Тип**: str or None
 - **description**: Способ усреднения for многоклассовой классификации
 - **Варианты**: 'binary', 'micro', 'macro', 'weighted', 'samples', None
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении F1-score
 - **`zero_division`**: Значение при делении on ноль (on умолчанию 'warn')
 - **Тип**: str or float
 - **description**: Что возвращать когда F1-score not определен
 - **Варианты**: 'warn', 0, 1
- **Возвращаемое значение**: float or array - F1-score for каждого класса
- **Типы усреднения**:
 - **`'binary'`**: for бинарной классификации (on умолчанию)
 - **`'micro'`**: Глобальное среднее (учитывает количество примеров)
 - **`'macro'`**: Среднее арифметическое on классам (равные веса)
 - **`'weighted'`**: Среднее взвешенное on количеству примеров
 - **`'samples'`**: F1-score for каждого примера отдельно
 - **`None`**: F1-score for каждого класса отдельно
- **Использование**:
 - **Баланс precision and recall**: Когда важны and точность, and полнота
 - **Несбалансированные данные**: F1-score хорошо работает with несбалансированными данными
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов

### Продвинутые метрики

#### ROC AUC
```python
# Площадь под ROC кривой
from sklearn.metrics import roc_auc_score

# for бинарной классификации
roc_auc = roc_auc_score(y_true, y_prob)
print(f"ROC AUC: {roc_auc:.4f}")

# for многоклассовой классификации
roc_auc_ovo = roc_auc_score(y_true, y_prob, multi_class='ovo')
roc_auc_ovr = roc_auc_score(y_true, y_prob, multi_class='ovr')
```

**🔧 Детальное description параметров roc_auc_score:**

**function roc_auc_score:**
- **Назначение**: Вычисление площади под ROC кривой (AUC)
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **Тип**: array-like
 - **description**: Массив истинных меток классов (0 and 1 for бинарной классификации)
 - **examples**: [0, 1, 1, 0, 1], [0, 1, 2, 0, 1] (многоклассовая)
 - **`y_score`**: Предсказанные вероятности or оценки
 - **Тип**: array-like
 - **description**: Массив вероятностей положительного класса or оценок
 - **examples**: [0.1, 0.9, 0.8, 0.2, 0.7]
 - **`average`**: Тип усреднения for многоклассовой классификации (on умолчанию 'macro')
 - **Тип**: str or None
 - **description**: Способ усреднения AUC for многоклассовой классификации
 - **Варианты**: 'macro', 'micro', 'weighted', 'samples', None
 - **`multi_class`**: Стратегия for многоклассовой классификации (on умолчанию 'raise')
 - **Тип**: str
 - **description**: Стратегия вычисления AUC for многоклассовой классификации
 - **Варианты**: 'raise', 'ovr', 'ovo'
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении AUC
 - **`max_fpr`**: Максимальный FPR for частичного AUC (on умолчанию None)
 - **Тип**: float or None
 - **description**: Максимальный False Positive Rate for вычисления частичного AUC
- **Возвращаемое значение**: float - AUC (0.0 - 1.0)
- **Стратегии многоклассовой классификации**:
 - **`'ovr'` (One-vs-Rest)**: Каждый класс против остальных
 - **Преимущества**: Быстрее вычисляется
 - **Недостатки**: Может быть неточным for несбалансированных данных
 - **`'ovo'` (One-vs-One)**: Каждый класс против каждого другого
 - **Преимущества**: Более точная оценка for несбалансированных данных
 - **Недостатки**: Вычислительно дороже
- **Использование**:
 - **Бинарная классификация**: Оценка качества разделения классов
 - **Несбалансированные данные**: ROC AUC устойчива к дисбалансу классов
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов

**🔧 Детальное description параметров ROC AUC:**

**Метрика ROC AUC:**
- **Что означает**: Площадь под кривой ROC (Receiver Operating Characteristic)
- **Формула**: `∫ TPR d(FPR)` где TPR = TP/(TP+FN), FPR = FP/(FP+TN)
- **Диапазон значений**: `[0, 1]` (0% - 100%)
- **Когда использовать**:
 - **Бинарная классификация**: Когда нужно оценить качество разделения классов
 - **Несбалансированные данные**: ROC AUC устойчива к дисбалансу классов
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
 - **Выбор порога**: Помогает найти оптимальный порог классификации
- **Когда not использовать**:
 - **Многоклассовая классификация**: Сложно интерпретировать
 - **Критичные ложные положиния**: not учитывает важность разных ошибок
- **Практические examples**:
 - **Отличное качество**: `> 0.9` (90%+)
 - **Хорошее качество**: `0.8-0.9` (80-90%)
 - **Приемлемое качество**: `0.7-0.8` (70-80%)
 - **Плохое качество**: `< 0.7` (< 70%)
- **parameter `multi_class`**:
 - **`'ovo'` (One-vs-One)**: Каждый класс сравнивается with каждым другим
 - **Преимущества**: Более точная оценка for несбалансированных данных
 - **Недостатки**: Вычислительно дороже for большого количества классов
 - **Когда использовать**: Когда классы несбалансированы
 - **`'ovr'` (One-vs-Rest)**: Каждый класс сравнивается with остальными
 - **Преимущества**: Быстрее вычисляется
 - **Недостатки**: Может быть неточным for несбалансированных данных
 - **Когда использовать**: Когда классы сбалансированы
- **parameter `average`** (for многоклассовой классификации):
 - **`'macro'`**: Среднее арифметическое on классам (равные веса)
 - **`'micro'`**: Глобальное среднее (веса on количеству примеров)
 - **`'weighted'`**: Среднее взвешенное on количеству примеров
- **Интерпретация значений**:
 - **0.5**: Случайное Prediction (модель not лучше случайного)
 - **0.7-0.8**: Приемлемое качество
 - **0.8-0.9**: Хорошее качество
 - **0.9+**: Отличное качество
 - **1.0**: Идеальное разделение классов

#### PR AUC
```python
# Площадь под Precision-Recall кривой
from sklearn.metrics import average_precision_score

pr_auc = average_precision_score(y_true, y_prob)
print(f"PR AUC: {pr_auc:.4f}")
```

**🔧 Детальное description параметров PR AUC:**

**Метрика PR AUC:**
- **Что означает**: Площадь под кривой Precision-Recall
- **Формула**: `∫ Precision d(Recall)` где Precision = TP/(TP+FP), Recall = TP/(TP+FN)
- **Диапазон значений**: `[0, 1]` (0% - 100%)
- **Когда использовать**:
 - **Несбалансированные данные**: PR AUC лучше ROC AUC for редких классов
 - **Критичные ложные положиния**: Когда ложные срабатывания дороги
 - **Медицинская диагностика**: Когда важна точность положительных предсказаний
 - **Обнаружение мошенничества**: Когда ложные обвинения дороги
- **Когда not использовать**:
 - **Сбалансированные данные**: ROC AUC может быть более информативной
 - **Критичные ложные отрицания**: Когда пропуски опаснее ложных срабатываний
- **Практические examples**:
 - **Отличное качество**: `> 0.8` (80%+)
 - **Хорошее качество**: `0.6-0.8` (60-80%)
 - **Приемлемое качество**: `0.4-0.6` (40-60%)
 - **Плохое качество**: `< 0.4` (< 40%)
- **Преимущества перед ROC AUC**:
 - **Несбалансированные данные**: Более информативна for редких классов
 - **Практическая интерпретация**: Прямо связана with precision and recall
 - **Чувствительность к дисбалансу**: Лучше отражает качество on редких классах
- **Недостатки**:
 - **Сложность интерпретации**: Менее интуитивна чем ROC AUC
 - **dependency from порога**: Может быть нестабильной при изменении порога
- **Интерпретация значений**:
 - **0.0**: Модель not лучше случайного
 - **0.3-0.5**: Приемлемое качество
 - **0.5-0.7**: Хорошее качество
 - **0.7+**: Отличное качество
 - **1.0**: Идеальное качество

#### Log Loss
```python
# Логарифмическая function потерь
from sklearn.metrics import log_loss

log_loss_score = log_loss(y_true, y_prob)
print(f"Log Loss: {log_loss_score:.4f}")
```

**🔧 Детальное description параметров Log Loss:**

**Метрика Log Loss:**
- **Что означает**: Логарифмическая function потерь (логистическая function потерь)
- **Формула**: `-1/N * Σ[y*log(p) + (1-y)*log(1-p)]` где p - вероятность положительного класса
- **Диапазон значений**: `[0, +∞)` (0 - идеальное Prediction, +∞ - худшее)
- **Когда использовать**:
 - **Оценка вероятностей**: Когда важны not только предсказания, но and их уверенность
 - **Несбалансированные данные**: Log Loss чувствительна к качеству вероятностей
 - **Сравнение моделей**: Хорошая метрика for сравнения качества вероятностей
 - **Калибровка модели**: Помогает оценить, насколько хорошо откалиброваны вероятности
- **Когда not использовать**:
 - **Только предсказания**: Когда важны только классы, а not вероятности
 - **Очень несбалансированные данные**: Может быть нестабильной
- **Практические examples**:
 - **Отличное качество**: `< 0.1` (очень низкая ошибка)
 - **Хорошее качество**: `0.1-0.3` (низкая ошибка)
 - **Приемлемое качество**: `0.3-0.5` (умеренная ошибка)
 - **Плохое качество**: `> 0.5` (высокая ошибка)
- **parameter `eps`** (on умолчанию 1e-15):
 - **Назначение**: Минимальное значение for вероятностей (избежание log(0))
 - **Рекомендуемые значения**: `1e-15` to `1e-7`
 - **Влияние**: Слишком большое значение может исказить результаты
- **parameter `normalize`** (on умолчанию True):
 - **True**: Нормализованная ошибка (деленная on количество примеров)
 - **False**: Суммарная ошибка
- **parameter `sample_weight`**:
 - **Назначение**: Веса for каждого примера
 - **Использование**: for несбалансированных данных or важных примеров
- **Интерпретация значений**:
 - **0.0**: Идеальное Prediction (вероятности точно соответствуют истине)
 - **0.1-0.3**: Очень хорошее качество
 - **0.3-0.5**: Хорошее качество
 - **0.5-0.7**: Приемлемое качество
 - **> 0.7**: Плохое качество
 - **+∞**: Худшее возможное Prediction

#### Balanced Accuracy
```python
# Сбалансированная точность for несбалансированных данных
from sklearn.metrics import balanced_accuracy_score

balanced_acc = balanced_accuracy_score(y_true, y_pred)
print(f"Balanced Accuracy: {balanced_acc:.4f}")
```

**🔧 Детальное description параметров Balanced Accuracy:**

**Метрика Balanced Accuracy:**
- **Что означает**: Среднее арифметическое чувствительности (recall) for каждого класса
- **Формула**: `(Sensitivity + Specificity) / 2` где Sensitivity = TP/(TP+FN), Specificity = TN/(TN+FP)
- **Диапазон значений**: `[0, 1]` (0% - 100%)
- **Когда использовать**:
 - **Несбалансированные данные**: Когда один класс значительно больше другого
 - **Медицинская диагностика**: Когда важны and ложные положиния, and ложные отрицания
 - **Обнаружение мошенничества**: Когда важны and пропуски, and ложные срабатывания
 - **Сравнение моделей**: Более справедливая оценка for несбалансированных данных
- **Когда not использовать**:
 - **Сбалансированные данные**: Обычная accuracy может быть более информативной
 - **Критичные ошибки**: Когда разные типы ошибок имеют разную важность
- **Практические examples**:
 - **Отличное качество**: `> 0.9` (90%+)
 - **Хорошее качество**: `0.8-0.9` (80-90%)
 - **Приемлемое качество**: `0.7-0.8` (70-80%)
 - **Плохое качество**: `< 0.7` (< 70%)
- **Преимущества перед обычной Accuracy**:
 - **Справедливость**: Каждый класс имеет равный вес
 - **Устойчивость к дисбалансу**: not зависит from распределения классов
 - **Интерпретируемость**: Показывает среднее качество on классам
- **Недостатки**:
 - **not учитывает важность**: Все классы считаются одинаково важными
 - **Может быть обманчивой**: Высокое значение not гарантирует хорошее качество
- **Интерпретация значений**:
 - **0.5**: Случайное Prediction (модель not лучше случайного)
 - **0.7-0.8**: Приемлемое качество
 - **0.8-0.9**: Хорошее качество
 - **0.9+**: Отличное качество
 - **1.0**: Идеальное качество

### Метрики for несбалансированных данных

#### Matthews Correlation Coefficient (MCC)
```python
from sklearn.metrics import matthews_corrcoef

mcc = matthews_corrcoef(y_true, y_pred)
print(f"MCC: {mcc:.4f}")
```

**🔧 Детальное description параметров MCC:**

**Метрика Matthews Correlation Coefficient (MCC):**
- **Что означает**: Корреляция между истинными and предсказанными классами
- **Формула**: `(TP*TN - FP*FN) / sqrt((TP+FP)*(TP+FN)*(TN+FP)*(TN+FN))`
- **Диапазон значений**: `[-1, 1]` (-1 - худшее, 0 - случайное, 1 - идеальное)
- **Когда использовать**:
 - **Несбалансированные данные**: MCC устойчива к дисбалансу классов
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
 - **Медицинская диагностика**: Когда важны все типы ошибок
 - **Обнаружение мошенничества**: Когда важны and пропуски, and ложные срабатывания
- **Когда not использовать**:
 - **Сбалансированные данные**: Может быть менее информативной чем accuracy
 - **Только предсказания**: Когда важны только классы, а not корреляция
- **Практические examples**:
 - **Отличное качество**: `> 0.7` (сильная корреляция)
 - **Хорошее качество**: `0.5-0.7` (умеренная корреляция)
 - **Приемлемое качество**: `0.3-0.5` (слабая корреляция)
 - **Плохое качество**: `< 0.3` (очень слабая корреляция)
- **Преимущества**:
 - **Устойчивость к дисбалансу**: not зависит from распределения классов
 - **Симметричность**: Одинаково чувствительна ко всем типам ошибок
 - **Интерпретируемость**: Показывает качество корреляции
- **Недостатки**:
 - **Сложность интерпретации**: Менее интуитивна чем accuracy
 - **Вычислительная сложность**: Требует больше вычислений
- **Интерпретация значений**:
 - **1.0**: Идеальная корреляция (все предсказания правильные)
 - **0.7-1.0**: Сильная корреляция
 - **0.5-0.7**: Умеренная корреляция
 - **0.3-0.5**: Слабая корреляция
 - **0.0**: Нет корреляции (случайное Prediction)
 - **-1.0**: Отрицательная корреляция (все предсказания неправильные)

#### Cohen's Kappa
```python
from sklearn.metrics import cohen_kappa_score

kappa = cohen_kappa_score(y_true, y_pred)
print(f"Cohen's Kappa: {kappa:.4f}")
```

**🔧 Детальное description параметров Cohen's Kappa:**

**Метрика Cohen's Kappa:**
- **Что означает**: Согласованность между истинными and предсказанными классами with учетом случайного согласия
- **Формула**: `(Po - Pe) / (1 - Pe)` где Po - наблюдаемое согласие, Pe - ожидаемое случайное согласие
- **Диапазон значений**: `[-1, 1]` (-1 - худшее, 0 - случайное, 1 - идеальное)
- **Когда использовать**:
 - **Несбалансированные данные**: Kappa учитывает случайное согласие
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
 - **Медицинская диагностика**: Когда важны все типы ошибок
 - **Обнаружение мошенничества**: Когда важны and пропуски, and ложные срабатывания
- **Когда not использовать**:
 - **Сбалансированные данные**: Может быть менее информативной чем accuracy
 - **Только предсказания**: Когда важны только классы, а not согласованность
- **Практические examples**:
 - **Отличное качество**: `> 0.8` (почти идеальное согласие)
 - **Хорошее качество**: `0.6-0.8` (существенное согласие)
 - **Приемлемое качество**: `0.4-0.6` (умеренное согласие)
 - **Плохое качество**: `< 0.4` (слабое согласие)
- **Преимущества**:
 - **Учет случайности**: Учитывает случайное согласие
 - **Устойчивость к дисбалансу**: not зависит from распределения классов
 - **Интерпретируемость**: Показывает качество согласованности
- **Недостатки**:
 - **Сложность интерпретации**: Менее интуитивна чем accuracy
 - **Вычислительная сложность**: Требует больше вычислений
- **Интерпретация значений**:
 - **1.0**: Идеальное согласие (все предсказания правильные)
 - **0.8-1.0**: Почти идеальное согласие
 - **0.6-0.8**: Существенное согласие
 - **0.4-0.6**: Умеренное согласие
 - **0.2-0.4**: Слабое согласие
 - **0.0**: Нет согласия (случайное Prediction)
 - **-1.0**: Отрицательное согласие (все предсказания неправильные)

## Метрики for регрессии

<img src="images/optimized/performance_comparison.png" alt="Метрики регрессии" style="max-width: 100%; height: auto; display: block; margin: 20px auto;">
*Рисунок 4: Метрики for задач регрессии and их интерпретация*

**Почему регрессия требует других метрик?** Потому что здесь мы предсказываем непрерывные значения, а not классы. Важна not только правильность, но and точность предсказаний.

### 🎯 Ключевые концепции метрик регрессии

**Почему важно понимать типы ошибок in регрессии?** Потому что разные метрики показывают разные аспекты качества:

- **MAE (Mean Absolute Error)**: Средняя абсолютная ошибка - простая and понятная метрика
- **MSE (Mean Squared Error)**: Среднеквадратичная ошибка - штрафует большие ошибки сильнее
- **RMSE (Root Mean Squared Error)**: Корень из MSE - in тех же единицах, что and целевая переменная
- **R² (Coefficient of Determination)**: Коэффициент детерминации - доля объясненной дисперсии
- **MAPE (Mean Absolute Percentage Error)**: Средняя абсолютная процентная ошибка
- **MAE vs MSE**: MAE менее чувствительна к выбросам, MSE сильнее штрафует большие ошибки

### Базовые метрики

#### Mean Absolute Error (MAE)
```python
from sklearn.metrics import mean_absolute_error

mae = mean_absolute_error(y_true, y_pred)
print(f"MAE: {mae:.4f}")
```

**🔧 Детальное description параметров mean_absolute_error:**

**function mean_absolute_error:**
- **Назначение**: Вычисление средней абсолютной ошибки (MAE)
- **parameters**:
 - **`y_true`**: Истинные значения
 - **Тип**: array-like
 - **description**: Массив истинных значений целевой переменной
 - **examples**: [1.5, 2.3, 3.1, 4.0], [100, 200, 150, 300]
 - **`y_pred`**: Предсказанные значения
 - **Тип**: array-like
 - **description**: Массив предсказанных значений
 - **examples**: [1.2, 2.5, 2.8, 4.2], [95, 210, 145, 320]
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении MAE
 - **examples**: [1.0, 2.0, 1.5, 0.5]
 - **`multioutput`**: Тип усреднения for многомерных выходов (on умолчанию 'uniform_average')
 - **Тип**: str or array-like
 - **description**: Способ усреднения for многомерных выходов
 - **Варианты**: 'uniform_average', 'raw_values', array весов
- **Возвращаемое значение**: float or array - MAE for каждого выхода
- **Типы усреднения for многомерных выходов**:
 - **`'uniform_average'`**: Равномерное усреднение on всем выходам
 - **`'raw_values'`**: Возвращает MAE for каждого выхода отдельно
 - **array весов**: Взвешенное усреднение with заданными весами
- **Использование**:
 - **Оценка точности**: Когда важна средняя ошибка предсказаний
 - **Устойчивость к выбросам**: MAE менее чувствительна к выбросам чем MSE
 - **Интерпретируемость**: Легко интерпретировать (средняя ошибка in единицах целевой переменной)

**🔧 Детальное description параметров MAE:**

**Метрика Mean Absolute Error (MAE):**
- **Что означает**: Средняя абсолютная ошибка между истинными and предсказанными значениями
- **Формула**: `(1/n) * Σ|y_true - y_pred|`
- **Диапазон значений**: `[0, +∞)` (0 - идеальное Prediction, +∞ - худшее)
- **Когда использовать**:
 - **Оценка точности**: Когда важна средняя ошибка предсказаний
 - **Устойчивость к выбросам**: MAE менее чувствительна к выбросам чем MSE
 - **Интерпретируемость**: Легко интерпретировать (средняя ошибка in единицах целевой переменной)
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
- **Когда not использовать**:
 - **Критичные большие ошибки**: Когда большие ошибки намного хуже маленьких
 - **Оптимизация**: MAE not дифференцируема in нуле
- **Практические examples**:
 - **Отличное качество**: `< 0.1 * std(y_true)` (очень низкая ошибка)
 - **Хорошее качество**: `0.1-0.3 * std(y_true)` (низкая ошибка)
 - **Приемлемое качество**: `0.3-0.5 * std(y_true)` (умеренная ошибка)
 - **Плохое качество**: `> 0.5 * std(y_true)` (высокая ошибка)
- **parameter `sample_weight`**:
 - **Назначение**: Веса for каждого примера
 - **Использование**: for важных примеров or несбалансированных данных
- **Преимущества**:
 - **Интерпретируемость**: Показывает среднюю ошибку in единицах целевой переменной
 - **Устойчивость к выбросам**: Менее чувствительна к экстремальным значениям
 - **Простота**: Легко понять and вычислить
- **Недостатки**:
 - **not дифференцируема**: not подходит for градиентной оптимизации
 - **not учитывает важность**: Все ошибки считаются одинаково важными
- **Интерпретация значений**:
 - **0.0**: Идеальное Prediction (все предсказания точные)
 - **< 0.1 * std**: Очень хорошее качество
 - **0.1-0.3 * std**: Хорошее качество
 - **0.3-0.5 * std**: Приемлемое качество
 - **> 0.5 * std**: Плохое качество

#### Mean Squared Error (MSE)
```python
from sklearn.metrics import mean_squared_error

mse = mean_squared_error(y_true, y_pred)
print(f"MSE: {mse:.4f}")
```

**🔧 Детальное description параметров mean_squared_error:**

**function mean_squared_error:**
- **Назначение**: Вычисление средней квадратичной ошибки (MSE)
- **parameters**:
 - **`y_true`**: Истинные значения
 - **Тип**: array-like
 - **description**: Массив истинных значений целевой переменной
 - **examples**: [1.5, 2.3, 3.1, 4.0], [100, 200, 150, 300]
 - **`y_pred`**: Предсказанные значения
 - **Тип**: array-like
 - **description**: Массив предсказанных значений
 - **examples**: [1.2, 2.5, 2.8, 4.2], [95, 210, 145, 320]
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении MSE
 - **examples**: [1.0, 2.0, 1.5, 0.5]
 - **`multioutput`**: Тип усреднения for многомерных выходов (on умолчанию 'uniform_average')
 - **Тип**: str or array-like
 - **description**: Способ усреднения for многомерных выходов
 - **Варианты**: 'uniform_average', 'raw_values', array весов
 - **`squared`**: Возвращать MSE or RMSE (on умолчанию True)
 - **Тип**: bool
 - **description**: True - возвращает MSE, False - возвращает RMSE
- **Возвращаемое значение**: float or array - MSE for каждого выхода
- **Типы усреднения for многомерных выходов**:
 - **`'uniform_average'`**: Равномерное усреднение on всем выходам
 - **`'raw_values'`**: Возвращает MSE for каждого выхода отдельно
 - **array весов**: Взвешенное усреднение with заданными весами
- **Использование**:
 - **Оптимизация**: MSE дифференцируема and подходит for градиентной оптимизации
 - **Критичные большие ошибки**: Когда большие ошибки намного хуже маленьких
 - **Обучение нейронных сетей**: Стандартная function потерь for регрессии

**🔧 Детальное description параметров MSE:**

**Метрика Mean Squared Error (MSE):**
- **Что означает**: Средняя квадратичная ошибка между истинными and предсказанными значениями
- **Формула**: `(1/n) * Σ(y_true - y_pred)²`
- **Диапазон значений**: `[0, +∞)` (0 - идеальное Prediction, +∞ - худшее)
- **Когда использовать**:
 - **Оптимизация**: MSE дифференцируема and подходит for градиентной оптимизации
 - **Критичные большие ошибки**: Когда большие ошибки намного хуже маленьких
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
 - **Обучение нейронных сетей**: Стандартная function потерь for регрессии
- **Когда not использовать**:
 - **Выбросы**: MSE очень чувствительна к экстремальным значениям
 - **Интерпретируемость**: Сложно интерпретировать (квадрат единиц целевой переменной)
- **Практические examples**:
 - **Отличное качество**: `< 0.01 * var(y_true)` (очень низкая ошибка)
 - **Хорошее качество**: `0.01-0.1 * var(y_true)` (низкая ошибка)
 - **Приемлемое качество**: `0.1-0.5 * var(y_true)` (умеренная ошибка)
 - **Плохое качество**: `> 0.5 * var(y_true)` (высокая ошибка)
- **parameter `sample_weight`**:
 - **Назначение**: Веса for каждого примера
 - **Использование**: for важных примеров or несбалансированных данных
- **Преимущества**:
 - **Дифференцируемость**: Подходит for градиентной оптимизации
 - **Чувствительность к большим ошибкам**: Большие ошибки штрафуются сильнее
 - **Математические свойства**: Имеет хорошие математические свойства
- **Недостатки**:
 - **Чувствительность к выбросам**: Очень чувствительна к экстремальным значениям
 - **Сложность интерпретации**: Сложно интерпретировать (квадрат единиц)
 - **Неравномерность штрафов**: Квадратичный штраф может быть слишком жестким
- **Интерпретация значений**:
 - **0.0**: Идеальное Prediction (все предсказания точные)
 - **< 0.01 * var**: Очень хорошее качество
 - **0.01-0.1 * var**: Хорошее качество
 - **0.1-0.5 * var**: Приемлемое качество
 - **> 0.5 * var**: Плохое качество

#### Root Mean Squared Error (RMSE)
```python
import numpy as np

rmse = np.sqrt(mean_squared_error(y_true, y_pred))
print(f"RMSE: {rmse:.4f}")
```

**🔧 Детальное description параметров RMSE:**

**Метрика Root Mean Squared Error (RMSE):**
- **Что означает**: Корень из средней квадратичной ошибки между истинными and предсказанными значениями
- **Формула**: `sqrt((1/n) * Σ(y_true - y_pred)²)`
- **Диапазон значений**: `[0, +∞)` (0 - идеальное Prediction, +∞ - худшее)
- **Когда использовать**:
 - **Интерпретируемость**: RMSE in тех же единицах что and целевая переменная
 - **Критичные большие ошибки**: Когда большие ошибки намного хуже маленьких
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
 - **Обучение нейронных сетей**: Стандартная метрика for регрессии
- **Когда not использовать**:
 - **Выбросы**: RMSE очень чувствительна к экстремальным значениям
 - **Неравномерные штрафы**: Квадратичный штраф может быть слишком жестким
- **Практические examples**:
 - **Отличное качество**: `< 0.1 * std(y_true)` (очень низкая ошибка)
 - **Хорошее качество**: `0.1-0.3 * std(y_true)` (низкая ошибка)
 - **Приемлемое качество**: `0.3-0.5 * std(y_true)` (умеренная ошибка)
 - **Плохое качество**: `> 0.5 * std(y_true)` (высокая ошибка)
- **Преимущества**:
 - **Интерпретируемость**: in тех же единицах что and целевая переменная
 - **Чувствительность к большим ошибкам**: Большие ошибки штрафуются сильнее
 - **Математические свойства**: Имеет хорошие математические свойства
- **Недостатки**:
 - **Чувствительность к выбросам**: Очень чувствительна к экстремальным значениям
 - **Неравномерность штрафов**: Квадратичный штраф может быть слишком жестким
 - **Сложность вычисления**: Требует вычисления квадратного корня
- **Интерпретация значений**:
 - **0.0**: Идеальное Prediction (все предсказания точные)
 - **< 0.1 * std**: Очень хорошее качество
 - **0.1-0.3 * std**: Хорошее качество
 - **0.3-0.5 * std**: Приемлемое качество
 - **> 0.5 * std**: Плохое качество

#### R² Score
```python
from sklearn.metrics import r2_score

r2 = r2_score(y_true, y_pred)
print(f"R² Score: {r2:.4f}")
```

**🔧 Детальное description параметров r2_score:**

**function r2_score:**
- **Назначение**: Вычисление коэффициента детерминации (R²)
- **parameters**:
 - **`y_true`**: Истинные значения
 - **Тип**: array-like
 - **description**: Массив истинных значений целевой переменной
 - **examples**: [1.5, 2.3, 3.1, 4.0], [100, 200, 150, 300]
 - **`y_pred`**: Предсказанные значения
 - **Тип**: array-like
 - **description**: Массив предсказанных значений
 - **examples**: [1.2, 2.5, 2.8, 4.2], [95, 210, 145, 320]
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении R²
 - **examples**: [1.0, 2.0, 1.5, 0.5]
 - **`multioutput`**: Тип усреднения for многомерных выходов (on умолчанию 'uniform_average')
 - **Тип**: str or array-like
 - **description**: Способ усреднения for многомерных выходов
 - **Варианты**: 'uniform_average', 'raw_values', array весов
- **Возвращаемое значение**: float or array - R² for каждого выхода
- **Типы усреднения for многомерных выходов**:
 - **`'uniform_average'`**: Равномерное усреднение on всем выходам
 - **`'raw_values'`**: Возвращает R² for каждого выхода отдельно
 - **array весов**: Взвешенное усреднение with заданными весами
- **Использование**:
 - **Оценка качества**: Когда нужно понять, насколько хорошо модель объясняет данные
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
 - **Интерпретируемость**: Легко интерпретировать (процент объясненной дисперсии)

**🔧 Детальное description параметров R² Score:**

**Метрика R² Score (Coefficient of Determination):**
- **Что означает**: Доля дисперсии целевой переменной, объясненная моделью
- **Формула**: `1 - (SS_res / SS_tot)` где SS_res = Σ(y_true - y_pred)², SS_tot = Σ(y_true - y_mean)²
- **Диапазон значений**: `(-∞, 1]` (1 - идеальное Prediction, 0 - как среднее, отрицательные - хуже среднего)
- **Когда использовать**:
 - **Оценка качества**: Когда нужно понять, насколько хорошо модель объясняет данные
 - **Сравнение моделей**: Хорошая метрика for сравнения разных алгоритмов
 - **Интерпретируемость**: Легко интерпретировать (процент объясненной дисперсии)
 - **Обучение нейронных сетей**: Стандартная метрика for регрессии
- **Когда not использовать**:
 - **Выбросы**: R² может быть обманчивым при наличии выбросов
 - **Неравномерные данные**: Может быть нестабильным for неравномерных данных
- **Практические examples**:
 - **Отличное качество**: `> 0.9` (90%+ объясненной дисперсии)
 - **Хорошее качество**: `0.7-0.9` (70-90% объясненной дисперсии)
 - **Приемлемое качество**: `0.5-0.7` (50-70% объясненной дисперсии)
 - **Плохое качество**: `< 0.5` (< 50% объясненной дисперсии)
- **parameter `sample_weight`**:
 - **Назначение**: Веса for каждого примера
 - **Использование**: for важных примеров or несбалансированных данных
- **Преимущества**:
 - **Интерпретируемость**: Показывает процент объясненной дисперсии
 - **Нормализация**: Значения from 0 to 1 легко сравнивать
 - **Математические свойства**: Имеет хорошие математические свойства
- **Недостатки**:
 - **Чувствительность к выбросам**: Может быть обманчивым при наличии выбросов
 - **Неравномерность**: Может быть нестабильным for неравномерных данных
 - **Сложность вычисления**: Требует вычисления среднего значения
- **Интерпретация значений**:
 - **1.0**: Идеальное Prediction (модель объясняет всю дисперсию)
 - **0.9-1.0**: Отличное качество
 - **0.7-0.9**: Хорошее качество
 - **0.5-0.7**: Приемлемое качество
 - **0.0-0.5**: Плохое качество
 - **0.0**: Модель not лучше среднего
 - **< 0.0**: Модель хуже среднего

### Продвинутые метрики

<img src="images/optimized/advanced_topics_overview.png" alt="Продвинутые метрики" style="max-width: 100%; height: auto; display: block; margin: 20px auto;">
*Рисунок 5: Продвинутые метрики for глубокого анализа качества моделей*

**Почему нужны продвинутые метрики?** Потому что базовые метрики not всегда показывают полную картину качества модели:

- **MAPE (Mean Absolute Percentage Error)**: Процентная ошибка for понимания относительной точности
- **SMAPE (Symmetric Mean Absolute Percentage Error)**: Симметричная версия MAPE
- **WAPE (Weighted Absolute Percentage Error)**: Взвешенная процентная ошибка
- **Custom Metrics**: Кастомные метрики for специфических задач
- **Business Metrics**: Бизнес-метрики, связанные with реальными KPI
- **Statistical Tests**: Статистические тесты for сравнения моделей

#### Mean Absolute Percentage Error (MAPE)
```python
def mape(y_true, y_pred):
 return np.mean(np.abs((y_true - y_pred) / y_true)) * 100

mape_score = mape(y_true, y_pred)
print(f"MAPE: {mape_score:.4f}%")
```

**🔧 Детальное description параметров MAPE:**

**Метрика Mean Absolute Percentage Error (MAPE):**
- **Что означает**: Средняя абсолютная процентная ошибка между истинными and предсказанными значениями
- **Формула**: `(1/n) * Σ|(y_true - y_pred) / y_true| * 100`
- **Диапазон значений**: `[0, +∞)` (0% - идеальное Prediction, +∞ - худшее)
- **Когда использовать**:
 - **Процентная интерпретация**: Когда важна процентная ошибка
 - **Сравнение разных масштабов**: MAPE нормализована and позволяет сравнивать разные данные
 - **Бизнес-метрики**: Легко интерпретировать for бизнеса
 - **Временные ряды**: Хорошая метрика for прогнозирования
- **Когда not использовать**:
 - **Нулевые значения**: MAPE not определена при y_true = 0
 - **Очень малые значения**: Может быть нестабильной при очень малых значениях
 - **Выбросы**: Очень чувствительна к экстремальным значениям
- **Практические examples**:
 - **Отличное качество**: `< 5%` (очень низкая ошибка)
 - **Хорошее качество**: `5-15%` (низкая ошибка)
 - **Приемлемое качество**: `15-30%` (умеренная ошибка)
 - **Плохое качество**: `> 30%` (высокая ошибка)
- **Преимущества**:
 - **Интерпретируемость**: Легко понять (процент ошибки)
 - **Нормализация**: Позволяет сравнивать разные данные
 - **Бизнес-применимость**: Хорошо подходит for бизнес-отчетов
- **Недостатки**:
 - **Проблема with нулями**: not определена при y_true = 0
 - **Чувствительность к выбросам**: Очень чувствительна к экстремальным значениям
 - **Асимметричность**: Штрафует занижение сильнее чем завышение
- **Интерпретация значений**:
 - **0%**: Идеальное Prediction (все предсказания точные)
 - **< 5%**: Очень хорошее качество
 - **5-15%**: Хорошее качество
 - **15-30%**: Приемлемое качество
 - **> 30%**: Плохое качество

#### Symmetric Mean Absolute Percentage Error (SMAPE)
```python
def smape(y_true, y_pred):
 return np.mean(2 * np.abs(y_true - y_pred) / (np.abs(y_true) + np.abs(y_pred))) * 100

smape_score = smape(y_true, y_pred)
print(f"SMAPE: {smape_score:.4f}%")
```

**🔧 Детальное description параметров SMAPE:**

**Метрика Symmetric Mean Absolute Percentage Error (SMAPE):**
- **Что означает**: Симметричная средняя абсолютная процентная ошибка между истинными and предсказанными значениями
- **Формула**: `(1/n) * Σ[2 * |y_true - y_pred| / (|y_true| + |y_pred|)] * 100`
- **Диапазон значений**: `[0, 200%]` (0% - идеальное Prediction, 200% - худшее)
- **Когда использовать**:
 - **Симметричная оценка**: Когда важна симметричная оценка ошибок
 - **Сравнение разных масштабов**: SMAPE нормализована and позволяет сравнивать разные данные
 - **Бизнес-метрики**: Легко интерпретировать for бизнеса
 - **Временные ряды**: Хорошая метрика for прогнозирования
- **Когда not использовать**:
 - **Нулевые значения**: SMAPE может быть нестабильной при y_true = 0 or y_pred = 0
 - **Очень малые значения**: Может быть нестабильной при очень малых значениях
 - **Выбросы**: Очень чувствительна к экстремальным значениям
- **Практические examples**:
 - **Отличное качество**: `< 10%` (очень низкая ошибка)
 - **Хорошее качество**: `10-20%` (низкая ошибка)
 - **Приемлемое качество**: `20-40%` (умеренная ошибка)
 - **Плохое качество**: `> 40%` (высокая ошибка)
- **Преимущества**:
 - **Симметричность**: Одинаково штрафует за завышение and занижение
 - **Интерпретируемость**: Легко понять (процент ошибки)
 - **Нормализация**: Позволяет сравнивать разные данные
 - **Бизнес-применимость**: Хорошо подходит for бизнес-отчетов
- **Недостатки**:
 - **Проблема with нулями**: Может быть нестабильной при y_true = 0 or y_pred = 0
 - **Чувствительность к выбросам**: Очень чувствительна к экстремальным значениям
 - **Сложность вычисления**: Требует больше вычислений чем MAPE
- **Интерпретация значений**:
 - **0%**: Идеальное Prediction (все предсказания точные)
 - **< 10%**: Очень хорошее качество
 - **10-20%**: Хорошее качество
 - **20-40%**: Приемлемое качество
 - **> 40%**: Плохое качество
 - **200%**: Худшее возможное Prediction

#### Mean Absolute Scaled Error (MASE)
```python
def mase(y_true, y_pred, y_train):
 # Наивный прогноз (следующее значение)
 naive_forecast = np.roll(y_train, 1)
 naive_mae = np.mean(np.abs(y_train - naive_forecast))

 # MAE модели
 model_mae = np.mean(np.abs(y_true - y_pred))

 return model_mae / naive_mae

mase_score = mase(y_true, y_pred, y_train)
print(f"MASE: {mase_score:.4f}")
```

**🔧 Детальное description параметров MASE:**

**Метрика Mean Absolute Scaled Error (MASE):**
- **Что означает**: Средняя абсолютная масштабированная ошибка относительно наивного прогноза
- **Формула**: `MAE_model / MAE_naive` где MAE_naive - MAE наивного прогноза
- **Диапазон значений**: `[0, +∞)` (0 - идеальное Prediction, 1 - как наивный, >1 - хуже наивного)
- **Когда использовать**:
 - **Временные ряды**: MASE специально разработана for временных рядов
 - **Сравнение with наивным прогнозом**: Когда важно понять, лучше ли модель наивного прогноза
 - **Нормализация**: MASE нормализована and позволяет сравнивать разные временные ряды
 - **Прогнозирование**: Хорошая метрика for оценки качества прогнозов
- **Когда not использовать**:
 - **not временные ряды**: MASE not подходит for обычной регрессии
 - **Очень короткие ряды**: Может быть нестабильной for коротких временных рядов
 - **Сезонные данные**: Может быть нестабильной for сильно сезонных данных
- **Практические examples**:
 - **Отличное качество**: `< 0.5` (in 2 раза лучше наивного)
 - **Хорошее качество**: `0.5-0.8` (лучше наивного)
 - **Приемлемое качество**: `0.8-1.0` (примерно как наивный)
 - **Плохое качество**: `> 1.0` (хуже наивного)
- **parameter `y_train`**:
 - **Назначение**: Обучающие данные for вычисления наивного прогноза
 - **Использование**: Должны быть те же данные, on которых обучалась модель
- **Преимущества**:
 - **Нормализация**: Позволяет сравнивать разные временные ряды
 - **Интерпретируемость**: Легко понять (лучше/хуже наивного прогноза)
 - **Устойчивость к масштабу**: not зависит from масштаба данных
 - **Специализация**: Специально разработана for временных рядов
- **Недостатки**:
 - **Ограниченность**: Подходит только for временных рядов
 - **dependency from данных**: Требует обучающие данные
 - **Сложность вычисления**: Требует вычисления наивного прогноза
- **Интерпретация значений**:
 - **0.0**: Идеальное Prediction (все предсказания точные)
 - **< 0.5**: Очень хорошее качество (in 2+ раза лучше наивного)
 - **0.5-0.8**: Хорошее качество (лучше наивного)
 - **0.8-1.0**: Приемлемое качество (примерно как наивный)
 - **1.0**: Как наивный прогноз
 - **> 1.0**: Хуже наивного прогноза

## Использование метрик in AutoGluon

### configuration метрик for обучения

```python
from autogluon.tabular import TabularPredictor

# for классификации
predictor = TabularPredictor(
 label='target',
 problem_type='binary',
 eval_metric='accuracy' # or 'f1', 'roc_auc', 'log_loss'
)

# for регрессии
predictor = TabularPredictor(
 label='target',
 problem_type='regression',
 eval_metric='rmse' # or 'mae', 'r2'
)
```

**🔧 Детальное description параметров TabularPredictor:**

**Класс TabularPredictor:**
- **Назначение**: create and обучение предиктора for табличных данных
- **parameters конструктора**:
 - **`label`**: Название целевой переменной
 - **Тип**: str
 - **description**: Название столбца with целевой переменной in данных
 - **examples**: 'target', 'price', 'category'
 - **`problem_type`**: Тип задачи (on умолчанию 'auto')
 - **Тип**: str
 - **description**: Тип задачи машинного обучения
 - **Варианты**: 'auto', 'binary', 'multiclass', 'regression', 'quantile'
 - **`eval_metric`**: Метрика for оценки (on умолчанию 'auto')
 - **Тип**: str or Scorer
 - **description**: Метрика for оптимизации во время обучения
 - **Варианты**: 'accuracy', 'f1', 'roc_auc', 'log_loss', 'rmse', 'mae', 'r2'
 - **`path`**: Путь for сохранения модели (on умолчанию 'AutogluonModels')
 - **Тип**: str
 - **description**: Директория for сохранения обученных моделей
 - **`verbosity`**: Уровень вывода (on умолчанию 2)
 - **Тип**: int
 - **description**: Уровень детализации вывода (0-4)
 - **Варианты**: 0 (тихо), 1 (минимально), 2 (нормально), 3 (подробно), 4 (максимально)
 - **`presets`**: Предустановки конфигурации (on умолчанию 'medium_quality_faster_train')
 - **Тип**: str
 - **description**: Предустановленные конфигурации for обучения
 - **Варианты**: 'best_quality', 'high_quality', 'good_quality', 'medium_quality', 'optimize_for_deployment'
- **Использование**:
 - **create предиктора**: Инициализация предиктора with заданными параметрами
 - **Обучение модели**: Вызов метода fit() for обучения
 - **Предсказания**: Вызов методов predict() and predict_proba()
 - **Оценка качества**: Вызов метода evaluate()

**🔧 Детальное description параметров eval_metric:**

**parameter eval_metric:**
- **Назначение**: Метрика for оптимизации во время обучения модели
- **Типы значений**: Строка with названием метрики or объект Scorer
- **Влияние on обучение**: AutoGluon использует эту метрику for выбора лучших моделей
- **Доступные метрики for классификации**:
 - **`'accuracy'`**: Точность (on умолчанию for multiclass)
 - **`'f1'`**: F1-score (on умолчанию for binary)
 - **`'roc_auc'`**: ROC AUC (хорошо for несбалансированных данных)
 - **`'log_loss'`**: Log Loss (хорошо for вероятностей)
 - **`'balanced_accuracy'`**: Сбалансированная точность
 - **`'precision'`**: Precision
 - **`'recall'`**: Recall
 - **`'mcc'`**: Matthews Correlation Coefficient
- **Доступные метрики for регрессии**:
 - **`'rmse'`**: Root Mean Squared Error (on умолчанию)
 - **`'mae'`**: Mean Absolute Error
 - **`'r2'`**: R² Score
 - **`'mse'`**: Mean Squared Error
 - **`'mape'`**: Mean Absolute Percentage Error
 - **`'smape'`**: Symmetric Mean Absolute Percentage Error
- **Рекомендации on выбору**:
 - **Сбалансированные данные**: `'accuracy'` for классификации, `'rmse'` for регрессии
 - **Несбалансированные данные**: `'f1'` or `'roc_auc'` for классификации
 - **Вероятности важны**: `'log_loss'` for классификации
 - **Процентные ошибки**: `'mape'` or `'smape'` for регрессии
 - **Временные ряды**: `'mae'` or `'rmse'` for регрессии

### Множественные метрики

```python
# Обучение with несколькими метриками
predictor.fit(
 train_data,
 eval_metric=['accuracy', 'f1', 'roc_auc']
)

# Получение всех метрик
performance = predictor.evaluate(test_data)
print(performance)
```

**🔧 Детальное description параметров метода evaluate:**

**Метод evaluate:**
- **Назначение**: Оценка качества обученной модели on тестовых данных
- **parameters**:
 - **`data`**: Тестовые данные
 - **Тип**: DataFrame
 - **description**: Таблица with тестовыми данными
 - **Структура**: Столбцы with приsignми + столбец with целевой переменной
 - **`metrics`**: Метрики for вычисления (on умолчанию None)
 - **Тип**: list or None
 - **description**: Список метрик for вычисления
 - **Варианты**: ['accuracy', 'f1', 'roc_auc'], None (все доступные)
 - **`silent`**: Подавлять вывод (on умолчанию False)
 - **Тип**: bool
 - **description**: Показывать ли прогресс вычисления метрик
- **Возвращаемое значение**: dict - словарь with метриками and их значениями
- **examples возвращаемых значений**:
 - **Классификация**: {'accuracy': 0.85, 'f1': 0.82, 'roc_auc': 0.88}
 - **Регрессия**: {'rmse': 0.15, 'mae': 0.12, 'r2': 0.78}
- **Использование**:
 - **Оценка качества**: Получение метрик качества on тестовых данных
 - **Сравнение моделей**: Сравнение разных алгоритмов
 - **Валидация**: check качества модели перед развертыванием
 - **Отчетность**: create отчетов о качестве модели
```

**🔧 Детальное description параметров метода fit:**

**Метод fit:**
- **Назначение**: Обучение предиктора on предоставленных данных
- **parameters**:
 - **`train_data`**: Обучающие данные
 - **Тип**: DataFrame
 - **description**: Таблица with обучающими данными
 - **Структура**: Столбцы with приsignми + столбец with целевой переменной
 - **`eval_metric`**: Метрика for оценки (on умолчанию None)
 - **Тип**: str, list or Scorer
 - **description**: Метрика or список метрик for оптимизации
 - **Варианты**: 'accuracy', 'f1', 'roc_auc', ['accuracy', 'f1'], custom_scorer
 - **`time_limit`**: Ограничение времени обучения (on умолчанию None)
 - **Тип**: int or None
 - **description**: Максимальное время обучения in секундах
 - **examples**: 300 (5 minutes), 3600 (1 час)
 - **`presets`**: Предустановки конфигурации (on умолчанию None)
 - **Тип**: str or None
 - **description**: Предустановленные конфигурации for обучения
 - **Варианты**: 'best_quality', 'high_quality', 'good_quality', 'medium_quality'
 - **`hyperparameters`**: Гиперпараметры моделей (on умолчанию None)
 - **Тип**: dict or None
 - **description**: Словарь with гиперпараметрами for разных алгоритмов
 - **examples**: {'GBM': {'num_boost_round': 100}, 'NN': {'epochs': 50}}
 - **`feature_metadata`**: Метаданные признаков (on умолчанию None)
 - **Тип**: FeatureMetadata or None
 - **description**: Метаданные о типах and свойствах признаков
 - **`holdout_frac`**: Доля данных for валидации (on умолчанию 0.1)
 - **Тип**: float
 - **description**: Доля данных for внутренней валидации (0.0 - 1.0)
 - **Рекомендации**: 0.1-0.2 for больших датасетов, 0.2-0.3 for малых
- **Возвращаемое значение**: None (модель сохраняется внутри предиктора)
- **Использование**:
 - **Обучение модели**: Основной метод for обучения предиктора
 - **configuration метрик**: Указание метрик for оптимизации
 - **Контроль времени**: Ограничение времени обучения
 - **configuration параметров**: Указание гиперпараметров for алгоритмов

**🔧 Детальное description параметров множественных метрик:**

**parameter eval_metric (список метрик):**
- **Назначение**: Список метрик for оценки качества модели
- **Типы значений**: Список строк with названиями метрик
- **Влияние on обучение**: AutoGluon использует первую метрику for оптимизации, остальные for мониторинга
- **Преимущества**:
 - **Комплексная оценка**: Позволяет оценить модель on нескольким критериям
 - **Мониторинг**: Помогает отслеживать качество во время обучения
 - **Сравнение**: Упрощает сравнение разных моделей
- **Рекомендации on выбору**:
 - **Основная метрика**: Первая in списке - for оптимизации
 - **Дополнительные метрики**: Остальные - for мониторинга
 - **Разнообразие**: Выбирайте метрики, которые измеряют разные аспекты качества
- **examples комбинаций**:
 - **Классификация**: `['accuracy', 'f1', 'roc_auc']`
 - **Регрессия**: `['rmse', 'mae', 'r2']`
 - **Несбалансированные данные**: `['f1', 'roc_auc', 'balanced_accuracy']`
 - **Вероятности**: `['log_loss', 'roc_auc', 'accuracy']`

### Кастомные метрики

```python
from autogluon.core import Scorer

# create кастомной метрики
def custom_metric(y_true, y_pred):
 """Кастомная метрика for оценки качества"""
 # Ваша логика расчета
 return score

custom_scorer = Scorer(
 name='custom_metric',
 score_func=custom_metric,
 greater_is_better=True
)

predictor.fit(
 train_data,
 eval_metric=custom_scorer
)
```

**🔧 Детальное description параметров кастомных метрик:**

**Класс Scorer:**
- **Назначение**: create пользовательских метрик for AutoGluon
- **parameters**:
 - **`name`**: Название метрики (строка)
 - **`score_func`**: function for вычисления метрики
 - **`greater_is_better`**: True если большее значение лучше, False если меньшее
- **function score_func**:
 - **Входные parameters**: `y_true`, `y_pred` (numpy arrays)
 - **Возвращаемое значение**: Число (float)
 - **Требования**: Должна быть векторизованной and работать with numpy arrays
- **parameter greater_is_better**:
 - **True**: Большее значение метрики означает лучшее качество (accuracy, f1, r2)
 - **False**: Меньшее значение метрики означает лучшее качество (mae, mse, log_loss)
- **examples кастомных метрик**:
 - **Бизнес-метрики**: Прибыль, убытки, конверсия
 - **Специфичные метрики**: for конкретных доменов
 - **Комбинированные метрики**: Объединение нескольких метрик
 - **Взвешенные метрики**: with учетом важности примеров
- **Рекомендации**:
 - **Простота**: function должна быть простой and быстрой
 - **Векторизация**: Используйте numpy операции for производительности
 - **Обработка ошибок**: Добавьте проверки on корректность входных данных
 - **documentation**: Хорошо документируйте назначение метрики

## Анализ производительности

### Лидерборд моделей

```python
# Получение лидерборда
leaderboard = predictor.leaderboard(test_data)
print(leaderboard)

# Детальный лидерборд
leaderboard_detailed = predictor.leaderboard(
 test_data,
 extra_info=True,
 silent=False
)
```

**🔧 Детальное description параметров метода leaderboard:**

**Метод leaderboard:**
- **Назначение**: Получение рейтинга моделей on качеству
- **parameters**:
 - **`data`**: Тестовые данные (on умолчанию None)
 - **Тип**: DataFrame or None
 - **description**: Таблица with тестовыми данными for оценки
 - **Структура**: Столбцы with приsignми + столбец with целевой переменной
 - **`extra_info`**: Дополнительная информация (on умолчанию False)
 - **Тип**: bool
 - **description**: Показывать ли дополнительную информацию о моделях
 - **Дополнительная информация**: Размер модели, количество признаков, гиперпараметры
 - **`silent`**: Подавлять вывод (on умолчанию True)
 - **Тип**: bool
 - **description**: Показывать ли прогресс вычисления метрик
- **Возвращаемое значение**: DataFrame - таблица with метриками for каждой модели
- **Столбцы лидерборда**:
 - **`model`**: Название модели
 - **`score_val`**: Значение основной метрики on валидации
 - **`score_test`**: Значение основной метрики on тесте (если передан data)
 - **`fit_time`**: Время обучения модели
 - **`pred_time`**: Время предсказания
 - **`stack_level`**: Уровень стекинга (for ансамблевых моделей)
- **Дополнительные столбцы (extra_info=True)**:
 - **`num_features`**: Количество признаков
 - **`memory_size`**: Размер модели in памяти
 - **`num_models`**: Количество моделей in ансамбле
 - **`hyperparameters`**: Гиперпараметры модели
- **Использование**:
 - **Сравнение моделей**: Выбор лучшей модели
 - **Анализ производительности**: Понимание сильных and слабых сторон
 - **Оптимизация**: Выбор параметров for дальнейшей settings

**🔧 Детальное description параметров лидерборда:**

**Метод leaderboard:**
- **Назначение**: Получение рейтинга моделей on качеству
- **parameters**:
 - **`test_data`**: Тестовые данные for оценки (опционально)
 - **`extra_info`**: Показывать дополнительную информацию (on умолчанию False)
 - **`silent`**: Подавлять вывод (on умолчанию True)
- **Возвращаемое значение**: DataFrame with метриками for каждой модели
- **Столбцы лидерборда**:
 - **`model`**: Название модели
 - **`score_val`**: Значение основной метрики on валидации
 - **`score_test`**: Значение основной метрики on тесте (если передан test_data)
 - **`fit_time`**: Время обучения модели
 - **`pred_time`**: Время предсказания
 - **`stack_level`**: Уровень стекинга (for ансамблевых моделей)
- **Дополнительная информация (extra_info=True)**:
 - **`num_features`**: Количество признаков
 - **`memory_size`**: Размер модели in памяти
 - **`num_models`**: Количество моделей in ансамбле
 - **`hyperparameters`**: Гиперпараметры модели
- **Использование**:
 - **Сравнение моделей**: Выбор лучшей модели
 - **Анализ производительности**: Понимание сильных and слабых сторон
 - **Оптимизация**: Выбор параметров for дальнейшей settings

### Анализ важности признаков

```python
# Важность признаков
feature_importance = predictor.feature_importance()
print(feature_importance)

# Визуализация важности признаков
import matplotlib.pyplot as plt

feature_importance.plot(kind='barh', figsize=(10, 8))
plt.title('Feature Importance')
plt.xlabel('Importance')
plt.show()
```

**🔧 Детальное description параметров метода feature_importance:**

**Метод feature_importance:**
- **Назначение**: Получение важности признаков for лучшей модели
- **parameters**:
 - **`data`**: Данные for вычисления важности (on умолчанию None)
 - **Тип**: DataFrame or None
 - **description**: Данные for вычисления важности признаков
 - **Структура**: Столбцы with приsignми + столбец with целевой переменной
 - **`model`**: Модель for analysis (on умолчанию None)
 - **Тип**: str or None
 - **description**: Название модели for analysis важности
 - **Варианты**: Название модели из лидерборда, None (лучшая модель)
 - **`subsample`**: Размер подвыборки (on умолчанию 10000)
 - **Тип**: int or None
 - **description**: Количество примеров for вычисления важности
 - **Рекомендации**: 10000 for быстрого вычисления, None for всех данных
 - **`num_shuffle_sets`**: Количество перестановок (on умолчанию 1)
 - **Тип**: int
 - **description**: Количество наборов перестановок for стабильности
 - **Рекомендации**: 1 for быстрого вычисления, 3-5 for стабильности
- **Возвращаемое значение**: Series - важность каждого приsign
- **Методы вычисления важности**:
 - **Permutation Importance**: Перестановка значений приsign and измерение падения качества
 - **SHAP Values**: Shapley Additive Explanations for объяснения предсказаний
 - **Tree-based Importance**: for деревьев решений (Gini, Information Gain)
- **Интерпретация значений**:
 - **Высокая важность**: Признак сильно влияет on предсказания
 - **Низкая важность**: Признак слабо влияет on предсказания
 - **Отрицательная важность**: Признак может вредить качеству модели
- **Использование**:
 - **Отбор признаков**: Выбор наиболее важных признаков
 - **Интерпретация модели**: Понимание логики работы модели
 - **Анализ данных**: Выявление наиболее информативных признаков
 - **Оптимизация**: improve качества данных

**🔧 Детальное description параметров анализа важности признаков:**

**Метод feature_importance:**
- **Назначение**: Получение важности признаков for лучшей модели
- **Возвращаемое значение**: Series with важностью каждого приsign
- **Методы вычисления важности**:
 - **Permutation Importance**: Перестановка значений приsign and измерение падения качества
 - **SHAP Values**: Shapley Additive Explanations for объяснения предсказаний
 - **Tree-based Importance**: for деревьев решений (Gini, Information Gain)
- **Интерпретация значений**:
 - **Высокая важность**: Признак сильно влияет on предсказания
 - **Низкая важность**: Признак слабо влияет on предсказания
 - **Отрицательная важность**: Признак может вредить качеству модели
- **Использование**:
 - **Отбор признаков**: Выбор наиболее важных признаков
 - **Интерпретация модели**: Понимание логики работы модели
 - **Анализ данных**: Выявление наиболее информативных признаков
 - **Оптимизация**: improve качества данных

### Анализ ошибок

```python
# Анализ ошибок for классификации
from sklearn.metrics import classification_report, confusion_matrix

# Отчет on классификации
print(classification_report(y_true, y_pred))

# Матрица ошибок
cm = confusion_matrix(y_true, y_pred)
print("Confusion Matrix:")
print(cm)

# Визуализация матрицы ошибок
import seaborn as sns
sns.heatmap(cm, annot=True, fmt='d', cmap='Blues')
plt.title('Confusion Matrix')
plt.show()
```

**🔧 Детальное description параметров classification_report:**

**function classification_report:**
- **Назначение**: Детальный отчет on качеству классификации
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **Тип**: array-like
 - **description**: Массив истинных меток классов
 - **examples**: [0, 1, 1, 0, 1], ['cat', 'dog', 'cat']
 - **`y_pred`**: Предсказанные метки классов
 - **Тип**: array-like
 - **description**: Массив предсказанных меток классов
 - **examples**: [0, 1, 0, 0, 1], ['cat', 'dog', 'dog']
 - **`target_names`**: Названия классов (on умолчанию None)
 - **Тип**: list or None
 - **description**: Список названий классов for отчета
 - **examples**: ['negative', 'positive'], ['cat', 'dog', 'bird']
 - **`labels`**: Список классов for отчета (on умолчанию None)
 - **Тип**: list or None
 - **description**: Список классов for включения in отчет
 - **examples**: [0, 1], ['cat', 'dog']
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении метрик
 - **`digits`**: Количество знаков после запятой (on умолчанию 2)
 - **Тип**: int
 - **description**: Количество знаков после запятой in выводе
 - **Рекомендации**: 2-4 for читаемости
 - **`zero_division`**: Значение при делении on ноль (on умолчанию 'warn')
 - **Тип**: str or float
 - **description**: Что возвращать когда метрика not определена
 - **Варианты**: 'warn', 0, 1
- **Возвращаемое значение**: str - текстовый отчет with метриками
- **Возвращаемые метрики**:
 - **Precision**: Точность for каждого класса
 - **Recall**: Полнота for каждого класса
 - **F1-score**: F1-мера for каждого класса
 - **Support**: Количество примеров in каждом классе
 - **Macro avg**: Среднее арифметическое on классам
 - **Weighted avg**: Среднее взвешенное on количеству примеров
- **Использование**:
 - **Детальный анализ**: Понимание качества on каждому классу
 - **Выявление проблем**: Поиск классов with плохим качеством
 - **Сравнение моделей**: Детальное сравнение разных алгоритмов

**🔧 Детальное description параметров confusion_matrix:**

**function confusion_matrix:**
- **Назначение**: Матрица ошибок for analysis типов ошибок
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **Тип**: array-like
 - **description**: Массив истинных меток классов
 - **examples**: [0, 1, 1, 0, 1], ['cat', 'dog', 'cat']
 - **`y_pred`**: Предсказанные метки классов
 - **Тип**: array-like
 - **description**: Массив предсказанных меток классов
 - **examples**: [0, 1, 0, 0, 1], ['cat', 'dog', 'dog']
 - **`labels`**: Список классов for матрицы (on умолчанию None)
 - **Тип**: list or None
 - **description**: Список классов for включения in матрицу
 - **examples**: [0, 1], ['cat', 'dog']
 - **`sample_weight`**: Веса for примеров (on умолчанию None)
 - **Тип**: array-like or None
 - **description**: Веса for каждого примера при вычислении матрицы
 - **`normalize`**: Нормализация матрицы (on умолчанию None)
 - **Тип**: str or None
 - **description**: Способ нормализации матрицы
 - **Варианты**: None, 'true', 'pred', 'all'
- **Возвращаемое значение**: ndarray - матрица ошибок
- **Элементы матрицы**:
 - **TP (True Positive)**: Правильно предсказанные положительные классы
 - **TN (True Negative)**: Правильно предсказанные отрицательные классы
 - **FP (False Positive)**: Неправильно предсказанные положительные классы
 - **FN (False Negative)**: Неправильно предсказанные отрицательные классы
- **Использование**:
 - **Анализ ошибок**: Понимание типов ошибок модели
 - **Калибровка**: configuration порога классификации
 - **Интерпретация**: Объяснение работы модели

**🔧 Детальное description параметров анализа ошибок:**

**function classification_report:**
- **Назначение**: Детальный отчет on качеству классификации
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **`y_pred`**: Предсказанные метки классов
 - **`target_names`**: Названия классов (опционально)
 - **`labels`**: Список классов for отчета (опционально)
 - **`sample_weight`**: Веса for примеров (опционально)
 - **`digits`**: Количество знаков после запятой (on умолчанию 2)
- **Возвращаемые метрики**:
 - **Precision**: Точность for каждого класса
 - **Recall**: Полнота for каждого класса
 - **F1-score**: F1-мера for каждого класса
 - **Support**: Количество примеров in каждом классе
 - **Macro avg**: Среднее арифметическое on классам
 - **Weighted avg**: Среднее взвешенное on количеству примеров
- **Использование**:
 - **Детальный анализ**: Понимание качества on каждому классу
 - **Выявление проблем**: Поиск классов with плохим качеством
 - **Сравнение моделей**: Детальное сравнение разных алгоритмов

**function confusion_matrix:**
- **Назначение**: Матрица ошибок for analysis типов ошибок
- **parameters**:
 - **`y_true`**: Истинные метки классов
 - **`y_pred`**: Предсказанные метки классов
 - **`labels`**: Список классов for матрицы (опционально)
 - **`sample_weight`**: Веса for примеров (опционально)
 - **`normalize`**: Нормализация матрицы (опционально)
- **Элементы матрицы**:
 - **TP (True Positive)**: Правильно предсказанные положительные классы
 - **TN (True Negative)**: Правильно предсказанные отрицательные классы
 - **FP (False Positive)**: Неправильно предсказанные положительные классы
 - **FN (False Negative)**: Неправильно предсказанные отрицательные классы
- **Использование**:
 - **Анализ ошибок**: Понимание типов ошибок модели
 - **Калибровка**: configuration порога классификации
 - **Интерпретация**: Объяснение работы модели

## Метрики for временных рядов

### Метрики for прогнозирования

```python
# Mean Absolute Scaled Error (MASE)
def mase_time_series(y_true, y_pred, y_train, seasonal_period=1):
 """MASE for временных рядов"""
 # Наивный прогноз
 naive_forecast = np.roll(y_train, seasonal_period)
 naive_mae = np.mean(np.abs(y_train - naive_forecast))

 # MAE модели
 model_mae = np.mean(np.abs(y_true - y_pred))

 return model_mae / naive_mae

# Symmetric Mean Absolute Percentage Error (SMAPE)
def smape_time_series(y_true, y_pred):
 """SMAPE for временных рядов"""
 return np.mean(2 * np.abs(y_true - y_pred) / (np.abs(y_true) + np.abs(y_pred))) * 100
```

**🔧 Детальное description параметров метрик временных рядов:**

**function mase_time_series:**
- **Назначение**: MASE for временных рядов with учетом сезонности
- **parameters**:
 - **`y_true`**: Истинные значения временного ряда
 - **`y_pred`**: Предсказанные значения
 - **`y_train`**: Обучающие данные for вычисления наивного прогноза
 - **`seasonal_period`**: Период сезонности (on умолчанию 1)
- **Особенности**:
 - **Сезонность**: Учитывает сезонные паттерны in данных
 - **Наивный прогноз**: Использует значение with предыдущего периода
 - **Нормализация**: Позволяет сравнивать разные временные ряды
- **Интерпретация**:
 - **< 0.5**: Модель in 2+ раза лучше наивного прогноза
 - **0.5-0.8**: Модель лучше наивного прогноза
 - **0.8-1.0**: Модель примерно как наивный прогноз
 - **> 1.0**: Модель хуже наивного прогноза

**function smape_time_series:**
- **Назначение**: SMAPE for временных рядов
- **parameters**:
 - **`y_true`**: Истинные значения временного ряда
 - **`y_pred`**: Предсказанные значения
- **Особенности**:
 - **Симметричность**: Одинаково штрафует за завышение and занижение
 - **Процентная интерпретация**: Легко понять (процент ошибки)
 - **Нормализация**: Позволяет сравнивать разные временные ряды
- **Интерпретация**:
 - **< 10%**: Очень хорошее качество
 - **10-20%**: Хорошее качество
 - **20-40%**: Приемлемое качество
 - **> 40%**: Плохое качество

### Метрики for финансовых данных

```python
# Sharpe Ratio
def sharpe_ratio(returns, risk_free_rate=0.02):
 """Коэффициент Шарпа"""
 excess_returns = returns - risk_free_rate
 return np.mean(excess_returns) / np.std(excess_returns)

# Maximum Drawdown
def max_drawdown(cumulative_returns):
 """Максимальная просадка"""
 peak = np.maximum.accumulate(cumulative_returns)
 drawdown = (cumulative_returns - peak) / peak
 return np.min(drawdown)

# Calmar Ratio
def calmar_ratio(returns, max_dd):
 """Коэффициент Калмара"""
 annual_return = np.mean(returns) * 252
 return annual_return / abs(max_dd)
```

**🔧 Детальное description параметров метрик финансовых данных:**

**function sharpe_ratio:**
- **Назначение**: Измерение доходности with учетом риска
- **parameters**:
 - **`returns`**: Массив доходностей (например, дневные доходности)
 - **`risk_free_rate`**: Безрисковая ставка (on умолчанию 0.02 = 2%)
- **Формула**: `(mean(returns) - risk_free_rate) / std(returns)`
- **Интерпретация**:
 - **> 1.0**: Отличная доходность with учетом риска
 - **0.5-1.0**: Хорошая доходность with учетом риска
 - **0.0-0.5**: Приемлемая доходность with учетом риска
 - **< 0.0**: Плохая доходность with учетом риска
- **Использование**:
 - **Сравнение стратегий**: Выбор лучшей торговой стратегии
 - **Оценка риска**: Понимание соотношения доходность/риск
 - **Оптимизация портфолио**: Балансировка риска and доходности

**function max_drawdown:**
- **Назначение**: Измерение максимальной просадки портфолио
- **parameters**:
 - **`cumulative_returns`**: Кумулятивные доходности (накопленные)
- **Формула**: `min((cumulative_returns - peak) / peak)`
- **Интерпретация**:
 - **> -0.1**: Очень низкая просадка (< 10%)
 - **-0.1 to -0.2**: Низкая просадка (10-20%)
 - **-0.2 to -0.3**: Умеренная просадка (20-30%)
 - **< -0.3**: Высокая просадка (> 30%)
- **Использование**:
 - **Управление рисками**: Контроль максимальных потерь
 - **Сравнение стратегий**: Выбор менее рискованных стратегий
 - **Оптимизация**: configuration параметров for снижения просадки

**function calmar_ratio:**
- **Назначение**: Измерение доходности относительно максимальной просадки
- **parameters**:
 - **`returns`**: Массив доходностей
 - **`max_dd`**: Максимальная просадка (отрицательное число)
- **Формула**: `annual_return / abs(max_dd)`
- **Интерпретация**:
 - **> 2.0**: Отличная доходность относительно просадки
 - **1.0-2.0**: Хорошая доходность относительно просадки
 - **0.5-1.0**: Приемлемая доходность относительно просадки
 - **< 0.5**: Плохая доходность относительно просадки
- **Использование**:
 - **Сравнение стратегий**: Выбор стратегий with лучшим соотношением доходность/просадка
 - **Оценка качества**: Понимание эффективности управления рисками
 - **Оптимизация**: configuration параметров for улучшения соотношения

## Мониторинг метрик

### Отслеживание метрик in реальном времени

```python
import logging
from datetime import datetime

class MetricsLogger:
 def __init__(self, log_file='metrics.log'):
 self.log_file = log_file
 self.metrics_history = []

 def log_metrics(self, metrics_dict):
 """Логирование метрик"""
 timestamp = datetime.now()
 metrics_dict['timestamp'] = timestamp
 self.metrics_history.append(metrics_dict)

 # Запись in файл
 with open(self.log_file, 'a') as f:
 f.write(f"{timestamp}: {metrics_dict}\n")

 def get_metrics_trend(self, metric_name):
 """Получение тренда метрики"""
 return [m[metric_name] for m in self.metrics_history if metric_name in m]

# Использование
metrics_logger = MetricsLogger()

# Логирование метрик
metrics = {
 'accuracy': 0.85,
 'f1_score': 0.82,
 'roc_auc': 0.88
}
metrics_logger.log_metrics(metrics)
```

**🔧 Детальное description параметров мониторинга метрик:**

**Класс MetricsLogger:**
- **Назначение**: Логирование and отслеживание метрик in реальном времени
- **parameters конструктора**:
 - **`log_file`**: Путь к файлу лога (on умолчанию 'metrics.log')
- **Методы**:
 - **`log_metrics(metrics_dict)`**: Логирование метрик
 - **`get_metrics_trend(metric_name)`**: Получение тренда метрики
- **parameters log_metrics**:
 - **`metrics_dict`**: Словарь with метриками and их значениями
- **parameters get_metrics_trend**:
 - **`metric_name`**: Название метрики for получения тренда
- **Возвращаемое значение**: Список значений метрики on времени
- **Использование**:
 - **Мониторинг качества**: Отслеживание изменения качества модели
 - **Выявление проблем**: Обнаружение деградации производительности
 - **Анализ трендов**: Понимание динамики метрик
 - **Отчетность**: create отчетов on качеству

### Алерты on метрикам

```python
class MetricsAlert:
 def __init__(self, threshold=0.8, metric_name='accuracy'):
 self.threshold = threshold
 self.metric_name = metric_name

 def check_alert(self, current_metric):
 """check алерта"""
 if current_metric < self.threshold:
 print(f"ALERT: {self.metric_name} = {current_metric} < {self.threshold}")
 return True
 return False

# Использование
alert = MetricsAlert(threshold=0.8, metric_name='accuracy')
if alert.check_alert(0.75):
 # Отправка уведомления
 pass
```

**🔧 Детальное description параметров алертов on метрикам:**

**Класс MetricsAlert:**
- **Назначение**: create алертов при падении качества метрик
- **parameters конструктора**:
 - **`threshold`**: Пороговое значение метрики (on умолчанию 0.8)
 - **`metric_name`**: Название метрики for мониторинга (on умолчанию 'accuracy')
- **Методы**:
 - **`check_alert(current_metric)`**: check алерта
- **parameters check_alert**:
 - **`current_metric`**: Текущее значение метрики
- **Возвращаемое значение**: True если алерт сработал, False если нет
- **Использование**:
 - **Мониторинг качества**: Автоматическое обнаружение проблем
 - **Уведомления**: Отправка алертов при падении качества
 - **Контроль**: Поддержание качества модели on заданном уровне
 - **Автоматизация**: Автоматическое реагирование on проблемы

## examples использования метрик

<img src="images/optimized/production_architecture.png" alt="examples использования метрик" style="max-width: 100%; height: auto; display: block; margin: 20px auto;">
*Рисунок 6: Практические examples использования метрик in реальных проектах*

**Почему важны практические examples?** Потому что они показывают, как применять метрики in реальных задачах:

- **Выбор метрик on типу задачи**: Классификация vs регрессия vs ранжирование
- **Интерпретация результатов**: Как понимать значения метрик
- **Сравнение моделей**: Как правильно сравнивать разные алгоритмы
- **Мониторинг in продакшене**: Как отслеживать качество in реальном времени
- **Бизнес-метрики**: Связь технических метрик with бизнес-результатами
- **A/B тестирование**: Как использовать метрики for тестирования

### Полный example оценки модели

```python
from autogluon.tabular import TabularPredictor
import pandas as pd
import numpy as np
from sklearn.datasets import make_classification
from sklearn.model_selection import train_test_split
import matplotlib.pyplot as plt
import seaborn as sns

# create данных
X, y = make_classification(
 n_samples=10000,
 n_features=20,
 n_informative=15,
 n_redundant=5,
 n_classes=2,
 random_state=42
)

data = pd.DataFrame(X, columns=[f'feature_{i}' for i in range(20)])
data['target'] = y

# Разделение данных
train_data, test_data = train_test_split(data, test_size=0.2, random_state=42)

# create and обучение модели
predictor = TabularPredictor(
 label='target',
 problem_type='binary',
 eval_metric='accuracy'
)

predictor.fit(train_data, time_limit=300)

# Предсказания
Predictions = predictor.predict(test_data)
probabilities = predictor.predict_proba(test_data)

# Оценка качества
performance = predictor.evaluate(test_data)
print("Performance Metrics:")
for metric, value in performance.items():
 print(f"{metric}: {value:.4f}")

# Лидерборд
leaderboard = predictor.leaderboard(test_data)
print("\nLeaderboard:")
print(leaderboard)

# Важность признаков
feature_importance = predictor.feature_importance()
print("\nFeature Importance:")
print(feature_importance.head(10))

# Визуализация
fig, axes = plt.subplots(2, 2, figsize=(15, 10))

# ROC кривая
from sklearn.metrics import roc_curve, auc
fpr, tpr, _ = roc_curve(test_data['target'], probabilities[1])
roc_auc = auc(fpr, tpr)
axes[0, 0].plot(fpr, tpr, label=f'ROC AUC = {roc_auc:.3f}')
axes[0, 0].plot([0, 1], [0, 1], 'k--')
axes[0, 0].set_xlabel('False Positive Rate')
axes[0, 0].set_ylabel('True Positive Rate')
axes[0, 0].set_title('ROC Curve')
axes[0, 0].legend()

# Precision-Recall кривая
from sklearn.metrics import precision_recall_curve
precision, recall, _ = precision_recall_curve(test_data['target'], probabilities[1])
axes[0, 1].plot(recall, precision)
axes[0, 1].set_xlabel('Recall')
axes[0, 1].set_ylabel('Precision')
axes[0, 1].set_title('Precision-Recall Curve')

# Матрица ошибок
from sklearn.metrics import confusion_matrix
cm = confusion_matrix(test_data['target'], Predictions)
sns.heatmap(cm, annot=True, fmt='d', cmap='Blues', ax=axes[1, 0])
axes[1, 0].set_title('Confusion Matrix')

# Важность признаков
feature_importance.head(10).plot(kind='barh', ax=axes[1, 1])
axes[1, 1].set_title('Top 10 Feature Importance')

plt.tight_layout()
plt.show()
```

## Следующие шаги

После освоения работы with метриками переходите к:
- [Методам валидации](./05_validation.md)
- [Продакшен деплою](./06_production.md)
- [Переобучению моделей](./07_retraining.md)
