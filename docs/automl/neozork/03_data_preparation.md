# 03. 📊 Подготовка данных

**Goal:** Научиться правильно подготавливать and очищать финансовые data for ML-моделей.

## Зачем нужна качественная подготовка данных?

**Theory:** Качество данных является фундаментальным фактором успеха ML-систем. in финансовой сфере это особенно критично, так как ошибки in данных могут привести к значительным финансовым потерям. Подготовка данных - это процесс трансформации сырых данных in формат, пригодный for machine learning.

**Качество данных = Качество модели**

**Почему качественная подготовка данных критична:**
- **Финансовые риски:** Плохие data могут привести к неправильным торговым решениям
- **Регуляторные требования:** Финансовые регуляторы требуют точности данных
- **Конкурентное преимущество:** Качественные data дают преимущество on рынке
- **Доверие пользователей:** Точные предсказания повышают доверие к системе

**Плохие data приводят к:**
- **Ложным сигналам:** Неправильные торговые сигналы из-за ошибок in данных
- **Переобучению:** Модель запоминает шум вместо реальных паттернов
- **Нестабильным результатам:** Непредсказуемое поведение системы
- **Потере денег:** Прямые финансовые потери from неправильных решений

**Плюсы качественной подготовки данных:**
- Повышение точности моделей
- Снижение рисков
- improve стабильности системы
- Повышение доверия пользователей

**Минусы качественной подготовки данных:**
- Высокие временные затраты
- Сложность процесса
- Необходимость экспертных знаний
- Высокие требования к вычислительным ресурсам

## Типы финансовых данных

**Theory:** Финансовые data имеют специфическую структуру and характеристики, которые необходимо учитывать при подготовке for ML-моделей. Понимание типов данных критично for создания эффективных признаков and предотвращения ошибок.

### 1. OHLCV data

**Theory:** OHLCV (Open, High, Low, Close, Volume) - это стандартный формат финансовых данных, который содержит основную информацию о торговых сессиях. Эти data являются основой for большинства финансовых ML-моделей.

**Почему OHLCV data важны:**
- **Полнота информации:** Содержат всю необходимую информацию о торговой сессии
- **Стандартизация:** Универсальный формат, поддерживаемый allи platformми
- **Историческая совместимость:** Позволяют Workingть with историческими данными
- **Технический анализ:** Основа for all технических indicators

**Плюсы OHLCV данных:**
- Полная информация о торговой сессии
- Стандартизированный формат
- Совместимость with техническими индикаторами
- Высокая точность данных

**Минусы OHLCV данных:**
- Могут содержать шум and выбросы
- Требуют обработки пропущенных значений
- Могут быть подвержены манипуляциям
- Ограниченная информация о внутридневной динамике
```python
# Structure OHLCV данных
data = {
 'Open': [100, 101, 102], # Цена открытия
 'High': [105, 106, 107], # Максимальная цена
 'Low': [99, 100, 101], # Минимальная цена
 'Close': [104, 105, 106], # Цена закрытия
 'Volume': [1000, 1200, 1100] # Объем торгов
}
```

### 2. Процентные изменения

**Theory:** Процентные изменения (returns) являются более стабильными for ML-моделей, чем абсолютные цены. Они нормализуют data and делают их более подходящими for machine learning.

**Почему процентные изменения лучше for ML:**
- **Стационарность:** Процентные изменения более стационарны, чем абсолютные цены
- **Нормализация:** Автоматическая нормализация данных
- **Сравнимость:** Позволяют сравнивать активы with разными ценами
- **Статистические свойства:** Лучше подходят for статистических методов

**Плюсы процентных изменений:**
- Более стабильные for ML-моделей
- Автоматическая нормализация
- Лучшие статистические свойства
- Сравнимость между активами

**Минусы процентных изменений:**
- Потеря информации об абсолютных уровнях
- Могут быть менее интерпретируемыми
- Требуют осторожности при обратном преобразовании
- Могут усиливать шум in данных
```python
# Более стабильные for ML
data['price_change'] = data['Close'].pct_change()
data['volume_change'] = data['Volume'].pct_change()
```

### 3. Technical индикаторы

**Theory:** Technical индикаторы - это математические преобразования ценовых данных, которые помогают выявить паттерны and тренды. Они являются основой for создания признаков in финансовых ML-моделях.

**Почему Technical индикаторы важны:**
- **Выявление паттернов:** Помогают выявить скрытые паттерны in данных
- **Фильтрация шума:** Сглаживают случайные колебания
- **Стандартизация:** Универсальные метрики for Analysis
- **Экспертное знание:** Включают опыт технических аналитиков

**Типы технических indicators:**
- **Трендовые:** SMA, EMA, MACD - показывают направление тренда
- **Осцилляторы:** RSI, Stochastic - показывают перекупленность/перепроданность
- **Волатильность:** Bollinger Bands, ATR - показывают волатильность
- **Объемные:** OBV, VWAP - учитывают объем торгов

**Плюсы технических indicators:**
- Выявление скрытых паттернов
- Фильтрация шума
- Стандартизированные метрики
- Включение экспертного знания

**Минусы технических indicators:**
- Могут быть запаздывающими
- Могут генерировать ложные сигналы
- Требуют Settings параметров
- Могут быть избыточными
```python
# RSI, MACD, Bollinger Bands
data['RSI'] = calculate_rsi(data['Close'])
data['MACD'] = calculate_macd(data['Close'])
data['BB_Upper'] = calculate_bollinger_upper(data['Close'])
```

## clean данных

**Theory:** clean данных - это критически важный этап подготовки данных for ML-моделей. Качественная clean данных может значительно улучшить производительность моделей and снизить риски.

**Почему clean данных важна:**
- **Качество модели:** Чистые data приводят к лучшим моделям
- **Снижение шума:** remove шума улучшает обучение
- **Предотвращение ошибок:** clean предотвращает ошибки in моделях
- **Повышение стабильности:** Чистые data делают модели более стабильными

### 1. remove дубликатов

**Theory:** Дубликаты in данных могут искажать результаты обучения and приводить к переобучению. remove дубликатов критично for финансовых данных, где дубликаты могут возникать из-за технических сбоев or ошибок in системах сбора данных.

**Почему дубликаты проблематичны:**
- **Искажение статистики:** Дубликаты искажают статистические свойства данных
- **Переобучение:** Модель может запомнить дублированные паттерны
- **Неравномерное распределение:** Дубликаты создают неравномерное распределение данных
- **Ложные корреляции:** Дубликаты могут создавать ложные корреляции

**Плюсы удаления дубликатов:**
- improve качества данных
- Предотвращение переобучения
- Более точные статистические свойства
- Снижение рисков

**Минусы удаления дубликатов:**
- Возможная потеря важной информации
- Сложность определения истинных дубликатов
- Риск удаления легитимных данных
- Необходимость экспертного Analysis
```python
def remove_duplicates(df):
 """remove дубликатов"""
 return df.drop_duplicates()
```

### 2. Обработка пропущенных значений

**Theory:** Пропущенные значения (Missing values) in финансовых данных могут возникать on различным причинам: Technical сбои, выходные дни, праздники, or Issues with источниками данных. Правильная обработка пропущенных значений критична for качества ML-моделей.

**Почему пропущенные значения проблематичны:**
- **Нарушение временных рядов:** Пропуски нарушают непрерывность временных рядов
- **Ошибки in вычислениях:** Многие алгоритмы not могут Workingть with пропущенными значениями
- **Искажение статистики:** Пропуски искажают статистические свойства данных
- **Снижение качества модели:** Модели, обученные on данных with пропусками, Workingют хуже

**Стратегии обработки пропущенных значений:**

**1. Forward Fill (ffill) - for ценовых данных:**
- **Принцип:** Использует последнее известное значение
- **Применение:** Идеально for ценовых данных, где пропуски обычно кратковременные
- **Преимущества:** Сохраняет Logsку временных рядов, not создает искусственных паттернов
- **Недостатки:** Может замаскировать реальные изменения in данных

**2. Interpolation - for объемных данных:**
- **Принцип:** Вычисляет промежуточные значения on basis соседних точек
- **Применение:** Подходит for объемных данных, где можно предположить плавные изменения
- **Преимущества:** Более точное восстановление данных, учитывает тренды
- **Недостатки:** Может создать нереалистичные значения

**3. Backward Fill (bfill) - for некоторых случаев:**
- **Принцип:** Использует следующее известное значение
- **Применение:** Когда известно, что пропуск произошел in конце периода
- **Преимущества:** Сохраняет актуальность данных
- **Недостатки:** Может создать ложные паттерны

**Плюсы правильной обработки пропущенных значений:**
- improve качества данных
- Сохранение целостности временных рядов
- Повышение точности моделей
- Предотвращение ошибок in вычислениях

**Минусы обработки пропущенных значений:**
- Возможное искажение реальных данных
- create искусственных паттернов
- Сложность выбора правильной стратегии
- Риск маскировки важных изменений

```python
def handle_Missing_values(df):
 """Обработка пропущенных значений

 Эта function применяет различные стратегии for обработки пропущенных значений
 in dependencies from типа данных:
 - Forward fill for ценовых данных (сохраняет Logsку временных рядов)
 - Interpolation for объемных данных (учитывает тренды)
 """
 # Forward fill for цен - Use последнее известное значение
 # Это Logsчно for цен, так как цена обычно not меняется резко
 df['Close'] = df['Close'].fillna(method='ffill')
 df['Open'] = df['Open'].fillna(method='ffill')
 df['High'] = df['High'].fillna(method='ffill')
 df['Low'] = df['Low'].fillna(method='ffill')

 # Interpolation for объемов - вычисляем промежуточные значения
 # Объемы могут изменяться более плавно, поэтому интерполяция подходит
 df['Volume'] = df['Volume'].interpolate()

 return df
```

### 3. remove выбросов

**Theory:** Выбросы (outliers) - это значения, которые значительно отличаются from остальных данных in наборе. in финансовых данных выбросы могут возникать из-за технических сбоев, ошибок ввода данных, or экстремальных рыночных events. Правильная обработка выбросов критична for качества ML-моделей.

**Почему выбросы проблематичны:**
- **Искажение статистики:** Выбросы сильно influence среднее значение and стандартное отклонение
- **Переобучение:** Модели могут запомнить выбросы вместо реальных паттернов
- **Снижение точности:** Выбросы ухудшают производительность большинства алгоритмов
- **Ложные сигналы:** Выбросы могут генерировать ложные торговые сигналы

**Типы выбросов in финансовых данных:**

**1. Technical выбросы:**
- **Причина:** Ошибки in системах сбора данных, Technical сбои
- **Характеристика:** Значения, которые физически невозможны (например, отрицательные цены)
- **Обработка:** Полное remove

**2. Рыночные выбросы:**
- **Причина:** Экстремальные рыночные события (крахи, паники)
- **Характеристика:** Реальные, но редкие события
- **Обработка:** Осторожное remove or специальная обработка

**3. Временные выбросы:**
- **Причина:** Праздники, выходные, нерабочие дни
- **Характеристика:** Отсутствие торгов or аномальная активность
- **Обработка:** Контекстная обработка

**Методы обнаружения выбросов:**

**1. Z-Score метод:**
- **Принцип:** Вычисляет стандартизированное отклонение from среднего
- **Формула:** Z = (x - μ) / σ
- **Порог:** Обычно 2-3 стандартных отклонения
- **Преимущества:** Простота, интерпретируемость
- **Недостатки:** Чувствительность к выбросам in вычислении статистик

**2. IQR (Interquartile Range) метод:**
- **Принцип:** Использует межквартильный размах
- **Формула:** Outlier если x < Q1 - 1.5*IQR or x > Q3 + 1.5*IQR
- **Преимущества:** Менее чувствителен к выбросам
- **Недостатки:** Может пропустить некоторые выбросы

**3. Изолированный лес (Isolation Forest):**
- **Принцип:** Машинное обучение for обнаружения аномалий
- **Преимущества:** Workingет with многомерными данными
- **Недостатки:** Сложность, требует Settings

**Плюсы удаления выбросов:**
- improve качества данных
- Повышение точности моделей
- Снижение влияния шума
- Более стабильные результаты

**Минусы удаления выбросов:**
- Возможная потеря важной информации
- Риск удаления легитимных данных
- Сложность определения истинных выбросов
- Потенциальная потеря рыночных сигналов

```python
def remove_outliers(df, threshold=3):
 """remove выбросов with помощью Z-score

 Эта function использует Z-score метод for обнаружения and удаления выбросов.
 Z-score показывает, on сколько стандартных отклонений значение отклоняется from среднего.

 parameters:
 - df: dataFrame with data
 - threshold: Пороговое значение Z-score (on умолчанию 3)

 Принцип работы:
 1. Вычисляет Z-score for каждого числового столбца
 2. Удаляет строки, где |Z-score| > threshold
 3. Возвращает очищенный dataFrame
 """
 # Получаем все числовые столбцы
 numeric_columns = df.select_dtypes(include=[np.number]).columns

 # Creating копию for безопасной работы
 df_clean = df.copy()

 # Обрабатываем каждый числовой столбец
 for col in numeric_columns:
 # Вычисляем Z-score: (значение - среднее) / стандартное отклонение
 z_scores = np.abs((df_clean[col] - df_clean[col].mean()) / df_clean[col].std())

 # Удаляем строки with Z-score больше порогового значения
 df_clean = df_clean[z_scores < threshold]

 return df_clean
```

## create признаков

### 1. Technical индикаторы

**Theory:** Technical индикаторы - это математические преобразования ценовых and объемных данных, которые помогают выявить скрытые паттерны, тренды and сигналы in финансовых данных. Они являются основой for создания признаков in финансовых ML-моделях and представляют собой кодированное экспертное знание технических аналитиков.

**Почему Technical индикаторы важны for ML:**
- **Выявление паттернов:** Помогают выявить скрытые паттерны, которые not видны in сырых данных
- **Фильтрация шума:** Сглаживают случайные колебания and выделяют значимые сигналы
- **Стандартизация:** Универсальные метрики for Analysis различных активов
- **Экспертное знание:** Включают многолетний опыт технических аналитиков
- **Нормализация:** Приводят data к сопоставимому масштабу

**Классификация технических indicators:**

**1. Трендовые индикаторы:**
- **SMA (Simple Moving Average):** Простое скользящее среднее - показывает общий тренд
- **EMA (Exponential Moving Average):** Экспоненциальное скользящее среднее - более чувствительно к недавним изменениям
- **MACD (Moving Average Convergence Divergence):** Схождение-расхождение скользящих средних - показывает изменения тренда

**2. Осцилляторы:**
- **RSI (Relative Strength index):** index относительной силы - показывает перекупленность/перепроданность
- **Stochastic:** Стохастический осциллятор - сравнивает текущую цену with диапазоном за период
- **Williams %R:** Процентный диапазон Уильямса - показывает положение цены in диапазоне

**3. Индикаторы волатильности:**
- **Bollinger Bands:** Полосы Боллинджера - показывают волатильность and уровни поддержки/сопротивления
- **ATR (Average True Range):** Средний истинный диапазон - показывает волатильность
- **Standard Deviation:** Стандартное отклонение - мера разброса цен

**4. Объемные индикаторы:**
- **OBV (On-Balance Volume):** Балансовый объем - показывает накопление/распределение
- **VWAP (Volume Weighted Average Price):** Средневзвешенная on объему цена
- **Volume Rate of Change:** Скорость изменения объема

**Принципы создания технических indicators:**

**1. Выбор периода:**
- **Короткие периоды (5-20):** Более чувствительны, больше сигналов, больше шума
- **Средние периоды (20-50):** Баланс между чувствительностью and стабильностью
- **Длинные периоды (50-200):** Более стабильны, меньше сигналов, меньше шума

**2. Комбинация indicators:**
- **Подтверждение:** Использование нескольких indicators for подтверждения сигналов
- **Дивергенция:** Различия in поведении indicators and цены
- **Синергия:** Комбинация indicators разных типов

**Плюсы технических indicators:**
- Выявление скрытых паттернов
- Фильтрация шума
- Стандартизированные метрики
- Включение экспертного знания
- improve качества признаков

**Минусы технических indicators:**
- Могут быть запаздывающими
- Могут генерировать ложные сигналы
- Требуют Settings параметров
- Могут быть избыточными
- dependency from исторических данных

```python
def create_Technical_indicators(df):
 """create технических indicators

 Эта function создает набор технических indicators for финансовых данных.
 Индикаторы помогают выявить паттерны and тренды, которые not видны in сырых данных.

 Создаваемые индикаторы:
 - SMA: Простые скользящие средние разных periods
 - EMA: Экспоненциальные скользящие средние
 - RSI: index относительной силы
 - MACD: Схождение-расхождение скользящих средних
 """

 # Скользящие средние (Simple Moving Average)
 # SMA сглаживает ценовые data and показывает общий тренд
 df['SMA_5'] = df['Close'].rolling(5).mean() # Краткосрочный тренд
 df['SMA_20'] = df['Close'].rolling(20).mean() # Среднесрочный тренд
 df['SMA_50'] = df['Close'].rolling(50).mean() # Long-term trend

 # Экспоненциальные средние (Exponential Moving Average)
 # EMA более чувствительны к недавним изменениям
 df['EMA_12'] = df['Close'].ewm(span=12).mean() # Быстрая EMA
 df['EMA_26'] = df['Close'].ewm(span=26).mean() # Медленная EMA

 # RSI (Relative Strength index)
 # Показывает перекупленность/перепроданность (0-100)
 df['RSI'] = calculate_rsi(df['Close'])

 # MACD (Moving Average Convergence Divergence)
 # Показывает изменения тренда
 df['MACD'] = df['EMA_12'] - df['EMA_26'] # Линия MACD
 df['MACD_signal'] = df['MACD'].ewm(span=9).mean() # Сигнальная линия

 return df
```

### 2. Статистические признаки

**Theory:** Статистические признаки - это математические характеристики данных, которые описывают их распределение, изменчивость and динамику. in финансовых данных статистические признаки помогают выявить рыночные условия, риски and возможности for trading.

**Почему статистические признаки важны for ML:**
- **describe рыночных условий:** Показывают текущее состояние рынка (спокойный, волатильный, трендовый)
- **Измерение рисков:** Помогают оценить уровень риска and неопределенности
- **Выявление аномалий:** Обнаруживают необычные рыночные события
- **Нормализация данных:** Приводят data к сопоставимому масштабу
- **improve predictions:** Добавляют контекстную информацию for моделей

**Типы статистических признаков:**

**1. Меры центральной тенденции:**
- **Среднее (Mean):** Центральное значение распределения
- **Медиана (Median):** Значение in середине упорядоченного ряда
- **Мода (Mode):** Наиболее часто встречающееся значение

**2. Меры изменчивости:**
- **Стандартное отклонение (Standard Deviation):** Мера разброса данных
- **Волатильность (Volatility):** Стандартное отклонение доходности
- **Размах (Range):** Разность между максимальным and минимальным значениями
- **Межквартильный размах (IQR):** Разность между 75-м and 25-м процентилями

**3. Меры формы распределения:**
- **Асимметрия (Skewness):** Мера асимметрии распределения
- **Эксцесс (Kurtosis):** Мера "остроты" распределения
- **Нормальность:** Соответствие нормальному распределению

**4. Меры динамики:**
- **Моментум (Momentum):** Скорость изменения цены
- **Rate of Change (ROC):** Процентное изменение за период
- **Ускорение (acceleration):** Изменение скорости изменения

**5. Меры корреляции:**
- **Автокорреляция:** Корреляция with предыдущими значениями
- **Кросс-корреляция:** Корреляция между разными активами
- **Корреляция with объемом:** Связь цены and объема

**Принципы создания статистических признаков:**

**1. Выбор окна:**
- **Короткие окна (5-20):** Более чувствительны к изменениям
- **Средние окна (20-50):** Баланс между чувствительностью and стабильностью
- **Длинные окна (50-200):** Более стабильны, меньше шума

**2. Нормализация:**
- **Z-score:** Стандартизация on среднему and стандартному отклонению
- **Min-Max:** Нормализация к диапазону [0, 1]
- **Robust scaling:** Использование медианы and IQR

**3. Обработка выбросов:**
- **Winsorization:** Ограничение экстремальных значений
- **Robust statistics:** Использование устойчивых к выбросам метрик
- **Outlier detection:** Обнаружение and обработка выбросов

**Плюсы статистических признаков:**
- Объективное describe данных
- Выявление рыночных условий
- Измерение рисков
- improve качества моделей
- Интерпретируемость

**Минусы статистических признаков:**
- dependency from исторических данных
- Могут быть запаздывающими
- Требуют Settings параметров
- Могут быть избыточными
- Чувствительность к выбросам

```python
def create_statistical_features(df):
 """create статистических признаков

 Эта function создает статистические признаки, которые описывают
 распределение, изменчивость and динамику финансовых данных.

 Создаваемые признаки:
 - Volatility: Волатильность (стандартное отклонение доходности)
 - Momentum: Моментум (отношение текущей цены к цене N periods назад)
 - ROC: Rate of Change (процентное изменение за период)
 """

 # Волатильность (Volatility)
 # Показывает изменчивость цены - ключевой показатель риска
 # Вычисляется как стандартное отклонение доходности за период
 df['Volatility'] = df['Close'].rolling(20).std()

 # Моментум (Momentum)
 # Показывает скорость изменения цены
 # Значения > 1 означают рост, < 1 - падение
 df['Momentum'] = df['Close'] / df['Close'].shift(10)

 # Rate of Change (ROC)
 # Показывает процентное изменение цены за период
 # Положительные значения - рост, отрицательные - падение
 df['ROC'] = df['Close'].pct_change(10)

 # Дополнительные статистические признаки

 # Среднее значение за период (тренд)
 df['Mean_20'] = df['Close'].rolling(20).mean()

 # Медиана за период (устойчивый тренд)
 df['Median_20'] = df['Close'].rolling(20).median()

 # Размах за период (волатильность)
 df['Range_20'] = df['Close'].rolling(20).max() - df['Close'].rolling(20).min()

 # Асимметрия распределения (skewness)
 df['Skewness_20'] = df['Close'].rolling(20).skew()

 # Эксцесс распределения (kurtosis)
 df['Kurtosis_20'] = df['Close'].rolling(20).kurt()

 return df
```

### 3. Временные признаки

**Theory:** Временные признаки (temporal features) - это признаки, которые кодируют временную информацию and dependencies in данных. in финансовых временных рядах текущие значения часто зависят from предыдущих значений, что делает временные признаки критически важными for ML-моделей.

**Почему временные признаки важны for ML:**
- **Автокорреляция:** Финансовые data имеют сильную автокорреляцию - текущие значения зависят from предыдущих
- **Тренды and циклы:** Временные признаки помогают выявить тренды and циклические паттерны
- **Инерция рынка:** Рынки имеют инерцию - текущие движения часто продолжаются
- **Сезонность:** Финансовые data могут иметь сезонные паттерны
- **Контекст:** Временные признаки предоставляют контекст for текущих значений

**Типы временных признаков:**

**1. Лаги (Lags):**
- **Принцип:** Использование предыдущих значений как признаков
- **Применение:** Показывает, как прошлые значения influence текущие
- **examples:** Close_lag_1, Close_lag_5, Close_lag_10
- **Интерпретация:** Прямая dependency from исторических значений

**2. Разности (Differences):**
- **Принцип:** Вычисление изменений между текущим and предыдущими значениями
- **Применение:** Показывает скорость and направление изменений
- **examples:** Close_diff_1, Close_diff_5
- **Интерпретация:** Моментум and ускорение изменений

**3. Скользящие окна (Rolling Windows):**
- **Принцип:** Агрегация данных за определенный период
- **Применение:** Сглаживание and выявление трендов
- **examples:** Rolling mean, rolling std, rolling min/max
- **Интерпретация:** Контекстная информация о периоде

**4. Экспоненциальные окна (Exponential Windows):**
- **Принцип:** Более высокий вес недавним данным
- **Применение:** Адаптация к изменениям in данных
- **examples:** EMA, exponential weighted std
- **Интерпретация:** Адаптивные тренды

**5. Временные метки (Time-based Features):**
- **Принцип:** Извлечение информации из временных меток
- **Применение:** Учет сезонности and циклов
- **examples:** День недели, месяц, квартал, час
- **Интерпретация:** Сезонные and циклические паттерны

**Принципы создания временных признаков:**

**1. Выбор лагов:**
- **Короткие лаги (1-5):** Непосредственная dependency
- **Средние лаги (5-20):** Среднесрочные паттерны
- **Длинные лаги (20-100):** Долгосрочные тренды

**2. Выбор разностей:**
- **Первая разность (diff=1):** Моментум
- **Вторая разность (diff=2):** Ускорение
- **Сезонные разности:** Учет сезонности

**3. Выбор окон:**
- **Короткие окна:** Более чувствительны к изменениям
- **Длинные окна:** Более стабильны, меньше шума
- **Адаптивные окна:** Изменяются in dependencies from волатильности

**Плюсы временных признаков:**
- Учет временных dependencies
- Выявление трендов and циклов
- improve предсказательной способности
- Интерпретируемость
- Гибкость Settings

**Минусы временных признаков:**
- Увеличение размерности данных
- Проблема мультиколлинеарности
- dependency from качества исторических данных
- Сложность выбора параметров
- Риск переобучения

```python
def create_time_features(df):
 """create временных признаков

 Эта function создает временные признаки, которые кодируют
 временную информацию and dependencies in финансовых данных.

 Создаваемые признаки:
 - Лаги: Предыдущие значения цены
 - Разности: Изменения между текущим and предыдущими значениями
 - Скользящие окна: Агрегация данных за период
 """

 # Лаги (Lags)
 # Use предыдущие значения как признаки
 # Это помогает модели понять, как прошлые значения influence текущие
 for lag in [1, 2, 3, 5, 10]:
 df[f'Close_lag_{lag}'] = df['Close'].shift(lag)

 # Разности (Differences)
 # Вычисляем изменения между текущим and предыдущими значениями
 # Это показывает скорость and направление изменений
 for diff in [1, 2, 5]:
 df[f'Close_diff_{diff}'] = df['Close'].diff(diff)

 # Дополнительные временные признаки

 # Скользящие средние разных periods
 for window in [5, 10, 20]:
 df[f'Close_ma_{window}'] = df['Close'].rolling(window).mean()

 # Скользящие стандартные отклонения
 for window in [5, 10, 20]:
 df[f'Close_std_{window}'] = df['Close'].rolling(window).std()

 # Скользящие минимумы and максимумы
 for window in [5, 10, 20]:
 df[f'Close_min_{window}'] = df['Close'].rolling(window).min()
 df[f'Close_max_{window}'] = df['Close'].rolling(window).max()

 # Процентные изменения за разные периоды
 for period in [1, 2, 5, 10]:
 df[f'Close_pct_{period}'] = df['Close'].pct_change(period)

 # Экспоненциальные скользящие средние
 for span in [5, 10, 20]:
 df[f'Close_ema_{span}'] = df['Close'].ewm(span=span).mean()

 return df
```

## Нормализация данных

### 1. StandardScaler

**Theory:** StandardScaler (стандартизация) - это метод нормализации данных, который преобразует признаки так, чтобы они имели среднее значение 0 and стандартное отклонение 1. Это один из самых популярных методов нормализации in машинном обучении.

**Почему StandardScaler важен:**
- **Масштабирование признаков:** Приводит все признаки к одинаковому масштабу
- **improve сходимости:** Ускоряет обучение алгоритмов, основанных on градиентном спуске
- **Предотвращение доминирования:** Предотвращает доминирование признаков with большими значениями
- **Совместимость with алгоритмами:** Многие алгоритмы ML требуют нормализованных данных
- **Стабильность:** Делает модели более стабильными and интерпретируемыми

**Принцип работы StandardScaler:**
- **Формула:** z = (x - μ) / σ
- **μ (mu):** Среднее значение приsign
- **σ (sigma):** Стандартное отклонение приsign
- **Результат:** Признаки with μ = 0 and σ = 1

**Когда использовать StandardScaler:**
- **Нормальное распределение:** Когда data приблизительно нормально распределены
- **Линейные алгоритмы:** SVM, Logsстическая регрессия, линейная регрессия
- **Нейронные сети:** for улучшения сходимости
- **Кластеризация:** K-means, иерархическая кластеризация
- **PCA:** for Analysis главных компонент

**Преимущества StandardScaler:**
- Простота and интерпретируемость
- Сохранение формы распределения
- Workingет with большинством алгоритмов
- Стандартизированный подход
- Хорошо Workingет with нормальными распределениями

**Недостатки StandardScaler:**
- Чувствительность к выбросам
- Может not Workingть with сильно скошенными распределениями
- Требует сохранения параметров for новых данных
- Может искажать исходное распределение

```python
from sklearn.preprocessing import StandardScaler

def standardize_data(df):
 """Стандартизация данных with помощью StandardScaler

 Эта function применяет стандартизацию (Z-score normalization) к числовым приsignм.
 Стандартизация преобразует data так, чтобы они имели среднее 0 and стандартное отклонение 1.

 Принцип работы:
 1. Вычисляет среднее and стандартное отклонение for каждого приsign
 2. Применяет формулу: z = (x - mean) / std
 3. Возвращает нормализованные data and объект scaler

 Возвращает:
 - df: dataFrame with нормализованными данными
 - scaler: Объект StandardScaler for обратного преобразования
 """
 # Creating объект StandardScaler
 scaler = StandardScaler()

 # Выбираем только числовые столбцы
 numeric_columns = df.select_dtypes(include=[np.number]).columns

 # Применяем стандартизацию к числовым столбцам
 # fit_transform вычисляет parameters and применяет преобразование
 df[numeric_columns] = scaler.fit_transform(df[numeric_columns])

 return df, scaler
```

### 2. MinMaxScaler

**Theory:** MinMaxScaler (минимум-максимум нормализация) - это метод нормализации данных, который масштабирует признаки к заданному диапазону, обычно [0, 1]. Этот метод сохраняет исходное распределение данных and особенно полезен, когда важно сохранить нулевые значения.

**Почему MinMaxScaler важен:**
- **Сохранение распределения:** Сохраняет исходную форму распределения данных
- **Фиксированный диапазон:** Приводит все признаки к одинаковому диапазону
- **Сохранение нулей:** Сохраняет нулевые значения (важно for разреженных данных)
- **Интерпретируемость:** Легко интерпретировать нормализованные значения
- **Совместимость:** Workingет with алгоритмами, чувствительными к масштабу

**Принцип работы MinMaxScaler:**
- **Формула:** X_scaled = (X - X_min) / (X_max - X_min)
- **X_min:** Минимальное значение приsign
- **X_max:** Максимальное значение приsign
- **Результат:** Признаки in диапазоне [0, 1]

**Когда использовать MinMaxScaler:**
- **Ограниченный диапазон:** Когда data имеют ограниченный диапазон
- **Сохранение нулей:** Когда важно сохранить нулевые значения
- **Нейронные сети:** for активационных функций with ограниченным диапазоном
- **Изображения:** for нормализации пикселей (0-255 → 0-1)
- **Процентные data:** Когда data уже in процентном формате

**Преимущества MinMaxScaler:**
- Сохранение исходного распределения
- Фиксированный диапазон [0, 1]
- Сохранение нулевых значений
- Простота интерпретации
- Хорошо Workingет with ограниченными данными

**Недостатки MinMaxScaler:**
- Чувствительность к выбросам
- Может сжимать data in узкий диапазон
- not Workingет with неограниченными распределениями
- Требует сохранения параметров
- Может искажать data with выбросами

```python
from sklearn.preprocessing import MinMaxScaler

def normalize_data(df):
 """Нормализация данных with помощью MinMaxScaler

 Эта function применяет минимум-максимум нормализацию к числовым приsignм.
 MinMaxScaler масштабирует data к диапазону [0, 1], сохраняя исходное распределение.

 Принцип работы:
 1. Находит минимальное and максимальное значения for каждого приsign
 2. Применяет формулу: (x - min) / (max - min)
 3. Возвращает нормализованные data and объект scaler

 Возвращает:
 - df: dataFrame with нормализованными данными (диапазон [0, 1])
 - scaler: Объект MinMaxScaler for обратного преобразования
 """
 # Creating объект MinMaxScaler
 scaler = MinMaxScaler()

 # Выбираем только числовые столбцы
 numeric_columns = df.select_dtypes(include=[np.number]).columns

 # Применяем нормализацию к числовым столбцам
 # fit_transform вычисляет min/max and применяет преобразование
 df[numeric_columns] = scaler.fit_transform(df[numeric_columns])

 return df, scaler
```

## create целевых переменных

### 1. Классификация направления

**Theory:** Классификация направления - это задача предсказания направления движения цены (вверх, вниз, без изменений) on определенный горизонт времени. Это одна из самых популярных задач in финансовом машинном обучении, так как она позволяет создавать торговые стратегии.

**Почему классификация направления важна:**
- **Торговые стратегии:** Позволяет создавать простые торговые сигналы (покупать/продавать/держать)
- **Management рисками:** Помогает оценивать вероятность различных исходов
- **Интерпретируемость:** Легко понять and объяснить результаты
- **Практичность:** Прямо применимо к реальной торговле
- **Сравнимость:** Позволяет сравнивать разные модели

**Типы классификации направления:**

**1. Бинарная классификация:**
- **Классы:** Вверх (1) / Вниз (0)
- **Порог:** Обычно 0% or небольшой порог (например, 0.1%)
- **Применение:** Простые торговые стратегии
- **Преимущества:** Простота, четкие сигналы
- **Недостатки:** Игнорирует небольшие изменения

**2. Трехклассовая классификация:**
- **Классы:** Вверх (2) / Без изменений (1) / Вниз (0)
- **Пороги:** Два порога (например, -0.1% and +0.1%)
- **Применение:** Более точные торговые стратегии
- **Преимущества:** Учет нейтральных состояний
- **Недостатки:** Сложность Settings порогов

**3. Многоклассовая классификация:**
- **Классы:** Несколько уровней изменения (например, 5-7 классов)
- **Пороги:** Несколько порогов
- **Применение:** Сложные торговые стратегии
- **Преимущества:** Высокая точность
- **Недостатки:** Сложность интерпретации

**Принципы создания целевых переменных:**

**1. Выбор горизонта:**
- **Краткосрочный (1-5 дней):** Более волатильные, больше шума
- **Среднесрочный (1-4 недели):** Баланс между точностью and практичностью
- **Долгосрочный (1-3 месяца):** Более стабильные, но менее практичные

**2. Выбор порогов:**
- **Фиксированные пороги:** Одинаковые for all periods
- **Адаптивные пороги:** Зависящие from волатильности
- **Процентильные пороги:** Основанные on историческом распределении

**3. Обработка нейтральных зон:**
- **Узкие зоны:** Больше сигналов, больше шума
- **Широкие зоны:** Меньше сигналов, меньше шума
- **Адаптивные зоны:** Зависящие from рыночных условий

**Плюсы классификации направления:**
- Простота интерпретации
- Прямая применимость к торговле
- Легкость сравнения моделей
- Management рисками
- Практичность

**Минусы классификации направления:**
- Потеря информации о величине изменений
- dependency from выбора порогов
- Игнорирование временных аспектов
- Сложность Settings
- Риск переобучения

```python
def create_direction_target(df, horizon=1):
 """create целевой переменной for классификации направления

 Эта function создает целевую переменную for предсказания направления движения цены.
 Используется трехклассовая классификация: вверх, без изменений, вниз.

 parameters:
 - df: dataFrame with data
 - horizon: Горизонт предсказания in днях (on умолчанию 1)

 Принцип работы:
 1. Вычисляет цену через horizon дней
 2. Вычисляет процентное изменение
 3. Классифицирует on порогам: < -0.1% (вниз), -0.1% to +0.1% (без изменений), > +0.1% (вверх)

 Возвращает:
 - target: Series with метками классов (0=вниз, 1=без изменений, 2=вверх)
 """
 # Получаем цену через horizon дней
 future_price = df['Close'].shift(-horizon)
 current_price = df['Close']

 # Вычисляем процентное изменение
 price_change = (future_price - current_price) / current_price

 # Классификация on порогам
 # -0.1% and +0.1% - пороги for нейтральной зоны
 target = pd.cut(
 price_change,
 bins=[-np.inf, -0.001, 0.001, np.inf], # -0.1% and +0.1%
 labels=[0, 1, 2], # 0=вниз, 1=без изменений, 2=вверх
 include_lowest=True
 )

 return target.astype(int)
```

### 2. Регрессия for цены

**Theory:** Регрессия for цены - это задача предсказания точного значения цены on определенный горизонт времени. in отличие from классификации направления, регрессия предсказывает конкретные числовые значения, что позволяет создавать более точные торговые стратегии.

**Почему регрессия цены важна:**
- **Точность predictions:** Предсказывает конкретные значения цены
- **Management рисками:** Позволяет оценивать размер потенциальных прибылей/убытков
- **Оптимизация позиций:** Помогает определять размер позиций
- **Стоп-лоссы and тейк-профиты:** Позволяет устанавливать точные уровни
- **Портфельное Management:** Помогает in распределении активов

**Типы регрессии цены:**

**1. Прямая регрессия цены:**
- **Целевая переменная:** Абсолютная цена через horizon дней
- **Преимущества:** Простота, интерпретируемость
- **Недостатки:** dependency from абсолютных уровней цен
- **Применение:** Краткосрочные предсказания

**2. Регрессия процентных изменений:**
- **Целевая переменная:** Процентное изменение цены
- **Преимущества:** Нормализация, сравнимость между активами
- **Недостатки:** Сложность интерпретации
- **Применение:** Среднесрочные предсказания

**3. Регрессия логарифмических изменений:**
- **Целевая переменная:** Логарифмическое изменение цены
- **Преимущества:** Симметричность, стационарность
- **Недостатки:** Сложность интерпретации
- **Применение:** Долгосрочные предсказания

**4. Регрессия волатильности:**
- **Целевая переменная:** Волатильность цены
- **Преимущества:** Management рисками
- **Недостатки:** Сложность измерения
- **Применение:** Риск-менеджмент

**Принципы создания целевых переменных for регрессии:**

**1. Выбор горизонта:**
- **Краткосрочный (1-5 дней):** Высокая точность, много шума
- **Среднесрочный (1-4 недели):** Баланс точности and стабильности
- **Долгосрочный (1-3 месяца):** Низкая точность, высокая стабильность

**2. Обработка выбросов:**
- **Winsorization:** Ограничение экстремальных значений
- **Robust scaling:** Использование устойчивых метрик
- **Outlier detection:** Обнаружение and обработка выбросов

**3. Нормализация:**
- **StandardScaler:** Стандартизация к μ=0, σ=1
- **MinMaxScaler:** Нормализация к [0, 1]
- **RobustScaler:** Устойчивая нормализация

**4. Временные аспекты:**
- **Стационарность:** check стационарности данных
- **Автокорреляция:** Учет временных dependencies
- **Сезонность:** Учет сезонных паттернов

**Плюсы регрессии цены:**
- Высокая точность predictions
- Конкретные числовые значения
- Management рисками
- Оптимизация торговых стратегий
- Гибкость применения

**Минусы регрессии цены:**
- Сложность интерпретации
- dependency from качества данных
- Риск переобучения
- Чувствительность к выбросам
- Сложность валидации

```python
def create_price_target(df, horizon=1):
 """create целевой переменной for регрессии цены

 Эта function создает целевую переменную for предсказания цены
 on определенный горизонт времени.

 parameters:
 - df: dataFrame with data
 - horizon: Горизонт предсказания in днях (on умолчанию 1)

 Принцип работы:
 1. Берет цену закрытия через horizon дней
 2. Возвращает как целевую переменную for регрессии

 Возвращает:
 - target: Series with целевыми значениями цены
 """
 # Получаем цену через horizon дней
 # shift(-horizon) сдвигает data назад, чтобы получить будущие значения
 return df['Close'].shift(-horizon)
```

## Валидация данных

### 1. check качества

**Theory:** Валидация качества данных - это критически важный процесс проверки данных on соответствие ожидаемым стандартам качества. in финансовом машинном обучении качество данных напрямую влияет on качество моделей and торговых решений.

**Почему валидация качества данных важна:**
- **Предотвращение ошибок:** Выявление проблем to обучения моделей
- **Повышение точности:** Качественные data приводят к лучшим моделям
- **Снижение рисков:** Предотвращение финансовых потерь from плохих данных
- **Соответствие стандартам:** Соответствие регуляторным требованиям
- **Доверие пользователей:** Повышение доверия к системе

**Типы проверок качества данных:**

**1. check полноты данных:**
- **Пропущенные значения:** Процент пропущенных значений in каждом столбце
- **Неполные записи:** Строки with частично заполненными данными
- **Временные пропуски:** Отсутствующие временные периоды
- **Критические поля:** check обязательных полей

**2. check точности данных:**
- **Выбросы:** Значения, значительно отличающиеся from ожидаемых
- **Невозможные значения:** Отрицательные цены, нулевые объемы
- **Logsческие ошибки:** High < Low, Close вне диапазона [Low, High]
- **Временные ошибки:** Неправильные временные метки

**3. check консистентности данных:**
- **Внутренняя консистентность:** Соответствие между связанными полями
- **Временная консистентность:** Logsческая последовательность во времени
- **Кросс-валидация:** Сравнение with внешними источниками
- **Бизнес-правила:** Соответствие финансовым правилам

**4. check актуальности данных:**
- **Свежесть данных:** Время последнего обновления
- **Задержки:** Время между событием and получением данных
- **Частота обновления:** Соответствие ожидаемой частоте
- **Источники данных:** Надежность источников

**Метрики качества данных:**

**1. Полнота (Completeness):**
- **Формула:** (Количество заполненных значений) / (Общее количество значений)
- **Порог:** Обычно > 95%
- **Интерпретация:** Процент данных, которые not являются пропущенными

**2. Точность (Accuracy):**
- **Формула:** (Количество правильных значений) / (Общее количество значений)
- **Порог:** Обычно > 99%
- **Интерпретация:** Процент данных, которые являются правильными

**3. Консистентность (Consistency):**
- **Формула:** (Количество консистентных записей) / (Общее количество записей)
- **Порог:** Обычно > 98%
- **Интерпретация:** Процент записей, которые not нарушают бизнес-правила

**4. Актуальность (Timeliness):**
- **Формула:** Время между событием and получением данных
- **Порог:** Зависит from требований (например, < 1 minutesы)
- **Интерпретация:** Свежесть данных

**Плюсы валидации качества данных:**
- Предотвращение ошибок
- Повышение точности моделей
- Снижение рисков
- Соответствие стандартам
- Повышение доверия

**Минусы валидации качества данных:**
- Временные затраты
- Сложность Settings
- Ложные срабатывания
- Необходимость экспертных знаний
- dependency from качества правил

```python
def validate_data_quality(df):
 """Валидация качества данных

 Эта function выполняет комплексную проверку качества финансовых данных,
 including проверку полноты, точности and консистентности.

 parameters:
 - df: dataFrame with data for проверки

 Проверки:
 1. Пропущенные значения - процент пропущенных значений in каждом столбце
 2. Выбросы - процент значений, отклоняющихся более чем on 3 сигмы
 3. Logsческие ошибки - check корректности OHLCV данных
 4. Временная консистентность - check временных меток

 Возвращает:
 - dict: Словарь with метриками качества данных
 """

 # 1. check on пропущенные значения
 # Вычисляем процент пропущенных значений for каждого столбца
 Missing_ratio = df.isnull().sum() / len(df)

 # 2. check on выбросы with помощью Z-score
 # Вычисляем процент значений, отклоняющихся более чем on 3 сигмы
 numeric_columns = df.select_dtypes(include=[np.number]).columns
 outlier_ratio = {}

 for col in numeric_columns:
 if df[col].std() > 0: # Избегаем деления on ноль
 z_scores = np.abs((df[col] - df[col].mean()) / df[col].std())
 outlier_ratio[col] = (z_scores > 3).sum() / len(df)
 else:
 outlier_ratio[col] = 0

 # 3. check Logsческих ошибок in OHLCV данных
 logical_errors = {}

 if 'High' in df.columns and 'Low' in df.columns:
 logical_errors['high_low_error'] = (df['High'] < df['Low']).sum() / len(df)

 if 'Open' in df.columns and 'Close' in df.columns and 'High' in df.columns and 'Low' in df.columns:
 logical_errors['ohlc_error'] = (
 (df['Open'] > df['High']) |
 (df['Open'] < df['Low']) |
 (df['Close'] > df['High']) |
 (df['Close'] < df['Low'])
 ).sum() / len(df)

 if 'Volume' in df.columns:
 logical_errors['negative_volume'] = (df['Volume'] < 0).sum() / len(df)

 # 4. check temporary консистентности
 time_consistency = {}

 if df.index.dtype == 'datetime64[ns]':
 # checking on дубликаты временных меток
 time_consistency['duplicate_timestamps'] = df.index.duplicated().sum() / len(df)

 # checking on пропуски во времени
 if len(df) > 1:
 time_diff = df.index.to_series().diff().dropna()
 expected_freq = time_diff.mode().iloc[0] if len(time_diff) > 0 else None
 if expected_freq is not None:
 time_consistency['time_gaps'] = (time_diff != expected_freq).sum() / len(time_diff)

 return {
 'Missing_ratio': Missing_ratio,
 'outlier_ratio': outlier_ratio,
 'logical_errors': logical_errors,
 'time_consistency': time_consistency
 }
```

### 2. check стабильности

**Theory:** check стабильности данных - это процесс Analysis временных рядов on предмет стационарности, стабильности статистических свойств and отсутствия структурных изменений. in финансовых данных стабильность критически важна for надежности ML-моделей.

**Почему check стабильности важна:**
- **Стационарность:** Многие ML-алгоритмы предполагают стационарность данных
- **Структурные изменения:** Выявление изменений in поведении данных
- **Надежность моделей:** Стабильные data приводят к более надежным моделям
- **Временная консистентность:** Обеспечение консистентности во времени
- **Предотвращение дрейфа:** Выявление дрейфа in данных

**Типы проверок стабильности:**

**1. check стационарности:**
- **ADF тест (Augmented Dickey-Fuller):** Проверяет presence единичного корня
- **KPSS тест:** Проверяет стационарность относительно тренда
- **PP тест (Phillips-Perron):** Альтернатива ADF тесту
- **Интерпретация:** p-value < 0.05 означает стационарность

**2. check структурных изменений:**
- **Chow тест:** Проверяет presence структурных изменений
- **CUSUM тест:** Выявляет накопленные изменения
- **Bai-Perron тест:** Определяет количество точек разрыва
- **Применение:** Выявление изменений in поведении данных

**3. check гомоскедастичности:**
- **White тест:** Проверяет постоянство дисперсии
- **Breusch-Pagan тест:** Альтернатива White тесту
- **Goldfeld-Quandt тест:** Проверяет гомоскедастичность
- **Интерпретация:** p-value > 0.05 означает гомоскедастичность

**4. check автокорреляции:**
- **Ljung-Box тест:** Проверяет отсутствие автокорреляции
- **Durbin-Watson тест:** Проверяет автокорреляцию первого порядка
- **Breusch-Godfrey тест:** Проверяет автокорреляцию высших порядков
- **Интерпретация:** p-value > 0.05 означает отсутствие автокорреляции

**Метрики стабильности данных:**

**1. Стационарность:**
- **ADF статистика:** Чем более отрицательная, тем более стационарны data
- **p-value:** < 0.05 означает стационарность
- **Критические значения:** Сравнение with критическими значениями

**2. Структурные изменения:**
- **F-статистика:** Чем больше, тем более вероятны структурные изменения
- **p-value:** < 0.05 означает presence структурных изменений
- **Точки разрыва:** Временные точки структурных изменений

**3. Гомоскедастичность:**
- **LM статистика:** Чем больше, тем более вероятна гетероскедастичность
- **p-value:** > 0.05 означает гомоскедастичность
- **R-squared:** Доля объясненной дисперсии

**4. Автокорреляция:**
- **Q-статистика:** Чем больше, тем более вероятна автокорреляция
- **p-value:** > 0.05 означает отсутствие автокорреляции
- **Durbin-Watson:** Значения близкие к 2 означают отсутствие автокорреляции

**Плюсы проверки стабильности:**
- Обеспечение надежности моделей
- Выявление структурных изменений
- Предотвращение дрейфа данных
- improve качества predictions
- Соответствие предположениям алгоритмов

**Минусы проверки стабильности:**
- Сложность интерпретации
- dependency from размера выборки
- Ложные срабатывания
- Необходимость экспертных знаний
- Временные затраты

```python
def check_data_stability(df):
 """check стабильности данных

 Эта function выполняет комплексную проверку стабильности финансовых данных,
 including проверку стационарности, структурных изменений and автокорреляции.

 parameters:
 - df: dataFrame with data for проверки

 Проверки:
 1. Стационарность - ADF тест for проверки единичного корня
 2. Структурные изменения - Chow тест for выявления разрывов
 3. Гомоскедастичность - White тест for проверки постоянства дисперсии
 4. Автокорреляция - Ljung-Box тест for проверки автокорреляции

 Возвращает:
 - dict: Словарь with результатами tests стабильности
 """

 # 1. check on стационарность with помощью ADF теста
 from statsmodels.tsa.stattools import adfuller
 from statsmodels.stats.diagnostic import acorr_ljungbox
 from statsmodels.stats.diagnostic import het_white
 from statsmodels.stats.stattools import durbin_watson

 # ADF тест for проверки стационарности
 adf_result = adfuller(df['Close'].dropna())

 # 2. check автокорреляции with помощью Ljung-Box теста
 # checking автокорреляцию in остатках (if present тренд)
 if len(df) > 10:
 try:
 # Вычисляем остатки (разности первого порядка)
 residuals = df['Close'].diff().dropna()
 ljung_box_result = acorr_ljungbox(residuals, lags=10, return_df=True)
 ljung_box_pvalue = ljung_box_result['lb_pvalue'].iloc[-1]
 except:
 ljung_box_pvalue = None
 else:
 ljung_box_pvalue = None

 # 3. check гомоскедастичности with помощью White теста
 # Creating простую регрессию for теста
 if len(df) > 20:
 try:
 # Creating лаги for регрессии
 df_test = df[['Close']].copy()
 df_test['Close_lag1'] = df_test['Close'].shift(1)
 df_test = df_test.dropna()

 if len(df_test) > 10:
 # Простая линейная регрессия
 from statsmodels.regression.linear_model import OLS
 from statsmodels.tools import add_constant

 X = add_constant(df_test[['Close_lag1']])
 y = df_test['Close']
 model = OLS(y, X).fit()

 # White тест
 white_result = het_white(model.resid, model.model.exog)
 white_pvalue = white_result[1]
 else:
 white_pvalue = None
 except:
 white_pvalue = None
 else:
 white_pvalue = None

 # 4. Durbin-Watson тест for автокорреляции первого порядка
 if len(df) > 10:
 try:
 dw_statistic = durbin_watson(df['Close'].diff().dropna())
 except:
 dw_statistic = None
 else:
 dw_statistic = None

 # 5. check структурных изменений (упрощенная версия)
 # Разделяем data пополам and сравниваем статистики
 if len(df) > 20:
 mid_point = len(df) // 2
 first_half = df['Close'].iloc[:mid_point]
 second_half = df['Close'].iloc[mid_point:]

 # Сравниваем средние значения
 mean_diff = abs(first_half.mean() - second_half.mean())
 std_ratio = first_half.std() / second_half.std() if second_half.std() > 0 else 1

 structural_change = {
 'mean_difference': mean_diff,
 'std_ratio': std_ratio,
 'significant_change': mean_diff > 2 * first_half.std()
 }
 else:
 structural_change = None

 return {
 'adf_statistic': adf_result[0],
 'adf_pvalue': adf_result[1],
 'is_stationary': adf_result[1] < 0.05,
 'ljung_box_pvalue': ljung_box_pvalue,
 'white_pvalue': white_pvalue,
 'durbin_watson': dw_statistic,
 'structural_change': structural_change
 }
```

## Сохранение данных

### 1. Parquet формат

**Theory:** Parquet - это колоночный формат files, optimized for аналитических рабочих нагрузок. Он обеспечивает высокую производительность чтения/записи, эффективное сжатие and поддержку сложных типов данных, что делает его идеальным for финансовых данных.

**Почему Parquet важен for финансовых данных:**
- **Высокая производительность:** Быстрое чтение and запись больших объемов данных
- **Эффективное сжатие:** Значительное уменьшение размера files
- **Колоночное хранение:** Оптимально for аналитических запросов
- **Схема данных:** Автоматическое сохранение типов данных
- **Совместимость:** Поддержка различными инструментами (Pandas, Spark, etc.)

**Преимущества Parquet формата:**

**1. Производительность:**
- **Колоночное хранение:** Чтение только нужных столбцов
- **Предикатная фильтрация:** Фильтрация on уровне файла
- **Векторизация:** Оптимизация for современных процессоров
- **Кэширование:** Эффективное использование памяти

**2. Сжатие:**
- **Алгоритмы сжатия:** Snappy, Gzip, LZ4, Brotli
- **Колоночное сжатие:** Лучшее сжатие for похожих данных
- **Адаптивное сжатие:** Выбор лучшего алгоритма for каждой колонки
- **Размер files:** Обычно in 5-10 раз меньше CSV

**3. Схема данных:**
- **Типы данных:** Автоматическое сохранение типов
- **Метаdata:** Встроенные метаdata о данных
- **Версионирование:** Поддержка эволюции схемы
- **Валидация:** check типов при чтении

**4. Совместимость:**
- **Pandas:** Нативная поддержка
- **Apache Spark:** Оптимизированная поддержка
- **DuckDB:** Высокая производительность
- **BigQuery:** Прямая загрузка

**Типы сжатия in Parquet:**

**1. Snappy (on умолчанию):**
- **Speed:** Очень быстрый
- **Сжатие:** Умеренное
- **Применение:** Интерактивные запросы

**2. Gzip:**
- **Speed:** Медленный
- **Сжатие:** Высокое
- **Применение:** Архивное хранение

**3. LZ4:**
- **Speed:** Быстрый
- **Сжатие:** Хорошее
- **Применение:** Баланс скорости and сжатия

**4. Brotli:**
- **Speed:** Медленный
- **Сжатие:** Очень высокое
- **Применение:** Долгосрочное хранение

**Плюсы Parquet формата:**
- Высокая производительность
- Эффективное сжатие
- Сохранение типов данных
- Широкая совместимость
- Оптимизация for аналитики

**Минусы Parquet формата:**
- Сложность for простых случаев
- dependency from библиотек
- Ограниченная поддержка in некоторых инструментах
- Требует понимания формата
- Может быть избыточным for малых данных

```python
def save_data_parquet(df, filename):
 """Сохранение данных in Parquet формате

 Эта function сохраняет dataFrame in Parquet формате with оптимизацией
 for финансовых данных. Parquet обеспечивает высокую производительность
 and эффективное сжатие.

 parameters:
 - df: dataFrame with data for сохранения
 - filename: Путь к файлу for сохранения

 Особенности:
 - Использует Snappy сжатие for баланса скорости and размера
 - Сохраняет типы данных and метаdata
 - Оптимизирован for аналитических запросов
 - Поддерживает большие объемы данных
 """
 # Сохраняем in Parquet формате with Snappy сжатием
 # Snappy обеспечивает хороший баланс между скоростью and сжатием
 df.to_parquet(filename, compression='snappy')

 # Дополнительные опции for оптимизации
 # df.to_parquet(
 # filename,
 # compression='snappy', # Быстрое сжатие
 # index=True, # Сохраняем index
 # engine='pyarrow', # Use PyArrow движок
 # schema=None # Автоматическое определение схемы
 # )
```

### 2. HDF5 формат

**Theory:** HDF5 (Hierarchical data Format version 5) - это высокопроизводительный формат files, предназначенный for хранения and organization больших объемов данных. Он обеспечивает быстрый доступ к данным, эффективное сжатие and поддержку метаданных, что делает его популярным for научных and финансовых приложений.

**Почему HDF5 важен for финансовых данных:**
- **Высокая производительность:** Быстрый доступ к большим объемам данных
- **Иерархическая Structure:** Организация данных in группы and наборы
- **Метаdata:** Встроенная поддержка метаданных
- **Сжатие:** Эффективное сжатие данных
- **Кроссплатформенность:** Workingет on различных операционных системах

**Преимущества HDF5 формата:**

**1. Производительность:**
- **Быстрый доступ:** Оптимизированный доступ к данным
- **Чтение on частям:** Возможность чтения только нужных данных
- **Кэширование:** Встроенное кэширование
- **Параллельный доступ:** Поддержка многопоточности

**2. Организация данных:**
- **Иерархическая Structure:** Группы and подгруппы
- **Метаdata:** Атрибуты for описания данных
- **indexация:** Быстрый поиск данных
- **Версионирование:** Отслеживание изменений

**3. Сжатие:**
- **Алгоритмы сжатия:** Gzip, LZF, SZIP
- **Адаптивное сжатие:** Выбор алгоритма for каждого набора данных
- **Размер files:** Значительное уменьшение размера
- **Speed:** Баланс между сжатием and скоростью

**4. Совместимость:**
- **Python:** H5py, PyTables, Pandas
- **R:** rhdf5
- **MATLAB:** Встроенная поддержка
- **C/C++:** Нативная поддержка

**Типы сжатия in HDF5:**

**1. Gzip (on умолчанию):**
- **Speed:** Медленный
- **Сжатие:** Высокое
- **Применение:** Архивное хранение

**2. LZF:**
- **Speed:** Быстрый
- **Сжатие:** Умеренное
- **Применение:** Интерактивные запросы

**3. SZIP:**
- **Speed:** Средний
- **Сжатие:** Высокое
- **Применение:** Научные data

**4. Без сжатия:**
- **Speed:** Очень быстрый
- **Сжатие:** Отсутствует
- **Применение:** Временные data

**Форматы хранения in HDF5:**

**1. Table формат:**
- **Structure:** Табличная Structure
- **Преимущества:** Легкий доступ к строкам
- **Недостатки:** Медленное чтение columns
- **Применение:** Временные ряды

**2. Fixed формат:**
- **Structure:** Фиксированная Structure
- **Преимущества:** Быстрое чтение
- **Недостатки:** Сложность изменения
- **Применение:** Статические data

**Плюсы HDF5 формата:**
- Высокая производительность
- Иерархическая организация
- Встроенные метаdata
- Эффективное сжатие
- Кроссплатформенность

**Минусы HDF5 формата:**
- Сложность for простых случаев
- dependency from библиотек
- Ограниченная поддержка in некоторых инструментах
- Требует понимания формата
- Может быть избыточным for малых данных

```python
def save_data_hdf5(df, filename):
 """Сохранение данных in HDF5 формате

 Эта function сохраняет dataFrame in HDF5 формате with оптимизацией
 for финансовых данных. HDF5 обеспечивает высокую производительность
 and эффективную организацию данных.

 parameters:
 - df: dataFrame with data for сохранения
 - filename: Путь к файлу for сохранения

 Особенности:
 - Использует table формат for оптимизации доступа
 - Сохраняет метаdata and атрибуты
 - Поддерживает сжатие данных
 - Обеспечивает быстрый доступ к данным
 """
 # Сохраняем in HDF5 формате with table структурой
 # table формат оптимизирован for временных рядов
 df.to_hdf(filename, 'data', mode='w', format='table')

 # Дополнительные опции for оптимизации
 # df.to_hdf(
 # filename,
 # 'data', # Имя группы in HDF5
 # mode='w', # Режим записи (w=write, a=append)
 # format='table', # Формат хранения
 # complevel=9, # Уровень сжатия (0-9)
 # complib='zlib', # Библиотека сжатия
 # fletcher32=True, # check целостности
 # data_columns=True # indexация columns
 # )
```

## Практический example

```python
import pandas as pd
import numpy as np
import yfinance as yf
from sklearn.preprocessing import StandardScaler

def prepare_trading_data(symbol, period='2y'):
 """Полная подготовка данных for trading"""

 # 1. Loading data
 ticker = yf.Ticker(symbol)
 df = ticker.history(period=period)

 # 2. clean
 df = remove_duplicates(df)
 df = handle_Missing_values(df)
 df = remove_outliers(df)

 # 3. create признаков
 df = create_Technical_indicators(df)
 df = create_statistical_features(df)
 df = create_time_features(df)

 # 4. create целевой переменной
 df['target'] = create_direction_target(df)

 # 5. remove NaN
 df = df.dropna()

 # 6. Нормализация
 scaler = StandardScaler()
 feature_columns = df.select_dtypes(include=[np.number]).columns
 df[feature_columns] = scaler.fit_transform(df[feature_columns])

 return df, scaler

# Использование
data, scaler = prepare_trading_data('BTC-USD')
print(f"Подготовлено {len(data)} записей with {data.shape[1]} приsignми")
```

## Следующие шаги

После подготовки данных переходите к:
- **[04_feature_engineering.md](04_feature_engineering.md)** - Инженерия признаков
- **[05_model_training.md](05_model_training.md)** - Обучение моделей

## Ключевые выводы

1. **Качество данных** критически важно for ML
2. **clean** должна быть тщательной
3. **Признаки** должны быть релевантными
4. **Нормализация** улучшает обучение
5. **Валидация** предотвращает ошибки

---

**Важно:** Потратьте время on качественную подготовку данных - это окупится in долгосрочной перспективе.
